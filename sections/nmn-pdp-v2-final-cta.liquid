{% comment %}
  NMN PDP alternative (product.13-2_nmn): Final CTA — Abschlussband vor Footer.
{% endcomment %}

{%- liquid
  assign primary_href = section.settings.primary_url
  if primary_href == blank
    assign primary_href = product.url
  endif
  assign secondary_href = section.settings.secondary_url
  if secondary_href == blank
    assign secondary_href = product.url
  endif
-%}

<section
  class="nmn2-final-cta"
  id="nmn2-final-cta-{{ section.id }}"
  aria-labelledby="nmn2-final-cta-heading-{{ section.id }}"
>
  <div class="container container--large">
    <div class="nmn2-final-cta__inner">
      {%- if section.settings.heading != blank -%}
        <h2 class="nmn2-final-cta__title" id="nmn2-final-cta-heading-{{ section.id }}">
          {{- section.settings.heading | escape -}}
        </h2>
      {%- endif -%}

      <ul class="nmn2-final-cta__bullets" role="list">
        {%- if section.settings.bullet_1 != blank -%}
          <li class="nmn2-final-cta__bullet">
            <span class="nmn2-final-cta__check" aria-hidden="true">
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <circle cx="12" cy="12" r="10"/>
                <path d="m9 12 2 2 4-4"/>
              </svg>
            </span>
            <span class="nmn2-final-cta__bullet-text">{{ section.settings.bullet_1 | escape }}</span>
          </li>
        {%- endif -%}
        {%- if section.settings.bullet_2 != blank -%}
          <li class="nmn2-final-cta__bullet">
            <span class="nmn2-final-cta__check" aria-hidden="true">
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <circle cx="12" cy="12" r="10"/>
                <path d="m9 12 2 2 4-4"/>
              </svg>
            </span>
            <span class="nmn2-final-cta__bullet-text">{{ section.settings.bullet_2 | escape }}</span>
          </li>
        {%- endif -%}
        {%- if section.settings.bullet_3 != blank -%}
          <li class="nmn2-final-cta__bullet">
            <span class="nmn2-final-cta__check" aria-hidden="true">
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <circle cx="12" cy="12" r="10"/>
                <path d="m9 12 2 2 4-4"/>
              </svg>
            </span>
            <span class="nmn2-final-cta__bullet-text">{{ section.settings.bullet_3 | escape }}</span>
          </li>
        {%- endif -%}
      </ul>

      {%- if section.settings.primary_label != blank and primary_href != blank -%}
        <a class="nmn2-final-cta__primary" href="{{ primary_href }}">{{ section.settings.primary_label | escape }}</a>
      {%- endif -%}

      {%- if section.settings.secondary_label != blank and secondary_href != blank -%}
        <a class="nmn2-final-cta__secondary" href="{{ secondary_href }}">{{ section.settings.secondary_label | escape }}</a>
      {%- endif -%}

      {%- if section.settings.footnote != blank -%}
        <p class="nmn2-final-cta__footnote">{{ section.settings.footnote | escape }}</p>
      {%- endif -%}
    </div>
  </div>
</section>

{% stylesheet %}
  .nmn2-final-cta {
    padding-block: var(--space-10);
    background: var(--color-surface-benefit);
    border-block: 1px solid var(--color-border-strong);
  }

  @media screen and (max-width: 749px) {
    .nmn2-final-cta {
      padding-block: var(--space-9);
    }
  }

  .nmn2-final-cta .container {
    display: flex;
    justify-content: center;
  }

  .nmn2-final-cta__inner {
    width: 100%;
    max-width: min(100%, 37.5rem);
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: var(--space-5);
  }

  .nmn2-final-cta__title {
    margin: 0;
    font-family: var(--font-heading);
    font-size: var(--text-h2);
    font-weight: var(--weight-bold);
    line-height: var(--leading-heading);
    color: var(--color-text-primary);
    text-wrap: balance;
  }

  .nmn2-final-cta__bullets {
    list-style: none;
    margin: 0;
    padding: 0;
    width: 100%;
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    text-align: left;
  }

  .nmn2-final-cta__bullet {
    margin: 0;
    display: flex;
    align-items: center;
    gap: var(--space-3);
  }

  .nmn2-final-cta__check {
    flex-shrink: 0;
    display: flex;
    color: var(--color-teal-dark);
  }

  .nmn2-final-cta__check svg {
    width: var(--space-4);
    height: var(--space-4);
  }

  .nmn2-final-cta__bullet-text {
    font-family: var(--font-body);
    font-size: var(--text-body);
    font-weight: var(--weight-regular);
    line-height: var(--leading-relaxed);
    color: var(--color-text-secondary);
  }

  .nmn2-final-cta__primary {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 100%;
    max-width: 25rem;
    margin-top: var(--space-2);
    padding: var(--space-4) var(--space-5);
    border-radius: var(--radius-md);
    font-family: var(--font-body);
    font-size: var(--text-body-sm);
    font-weight: var(--weight-semibold);
    line-height: var(--leading-tight);
    text-align: center;
    text-decoration: none;
    background: var(--color-button-primary-bg);
    color: var(--color-button-primary-text);
    transition: opacity var(--ease-default);
  }

  @media screen and (min-width: 750px) {
    .nmn2-final-cta__primary {
      font-size: var(--text-body);
    }
  }

  .nmn2-final-cta__primary:hover {
    opacity: 0.92;
  }

  .nmn2-final-cta__primary:focus-visible {
    outline: 2px solid var(--color-teal-dark);
    outline-offset: var(--space-2);
  }

  .nmn2-final-cta__secondary {
    font-family: var(--font-body);
    font-size: var(--text-body-sm);
    font-weight: var(--weight-medium);
    line-height: var(--leading-body);
    color: var(--color-teal-dark);
    text-decoration: underline;
    text-underline-offset: var(--space-1);
    transition: color var(--ease-default);
  }

  .nmn2-final-cta__secondary:hover,
  .nmn2-final-cta__secondary:focus-visible {
    color: var(--color-dark);
    outline: none;
  }

  .nmn2-final-cta__secondary:focus-visible {
    box-shadow: 0 0 0 2px var(--color-surface-benefit), 0 0 0 4px var(--color-teal-dark);
    border-radius: var(--radius-sm);
  }

  .nmn2-final-cta__footnote {
    margin: var(--space-1) 0 0;
    font-family: var(--font-body);
    font-size: var(--text-micro);
    font-weight: var(--weight-regular);
    line-height: var(--leading-body);
    color: var(--color-text-muted);
  }
{% endstylesheet %}

{% schema %}
{
  "name": "NMN V2 Final CTA",
  "tag": "section",
  "class": "section-nmn2-final-cta",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Überschrift",
      "default": "Starte deine Longevity-Routine — noch heute"
    },
    {
      "type": "text",
      "id": "bullet_1",
      "label": "Bullet 1",
      "default": "Uthever® NMN >99,9% rein"
    },
    {
      "type": "text",
      "id": "bullet_2",
      "label": "Bullet 2",
      "default": "Arzt-geführt aus Berlin · unabhängig laborgeprüft"
    },
    {
      "type": "text",
      "id": "bullet_3",
      "label": "Bullet 3",
      "default": "90-Tage Geld-zurück-Garantie ohne Wenn und Aber"
    },
    {
      "type": "text",
      "id": "primary_label",
      "label": "Primär-Button Text",
      "default": "Abo starten — 34,90 €/Monat · jederzeit flexibel"
    },
    {
      "type": "text",
      "id": "primary_url",
      "label": "Primär-Link (optional)",
      "info": "Leer = aktuelle Produktseite."
    },
    {
      "type": "text",
      "id": "secondary_label",
      "label": "Sekundär-Link Text",
      "default": "Oder einmalig kaufen — 39,90 €"
    },
    {
      "type": "text",
      "id": "secondary_url",
      "label": "Sekundär-Link (optional)",
      "info": "Leer = aktuelle Produktseite."
    },
    {
      "type": "text",
      "id": "footnote",
      "label": "Kleine Zeile darunter",
      "default": "Risikolos testen · 90-Tage Garantie"
    }
  ],
  "presets": [
    {
      "name": "NMN V2 Final CTA",
      "settings": {
        "heading": "Starte deine Longevity-Routine — noch heute",
        "bullet_1": "Uthever® NMN >99,9% rein",
        "bullet_2": "Arzt-geführt aus Berlin · unabhängig laborgeprüft",
        "bullet_3": "90-Tage Geld-zurück-Garantie ohne Wenn und Aber",
        "primary_label": "Abo starten — 34,90 €/Monat · jederzeit flexibel",
        "secondary_label": "Oder einmalig kaufen — 39,90 €",
        "footnote": "Risikolos testen · 90-Tage Garantie"
      }
    }
  ]
}
{% endschema %}
