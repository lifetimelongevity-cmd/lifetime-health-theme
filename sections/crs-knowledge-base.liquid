{%- liquid
  assign crs_h_align = section.settings.content_align | default: 'left'
  if crs_h_align != 'left' and crs_h_align != 'center' and crs_h_align != 'right'
    assign crs_h_align = 'left'
  endif
  assign crs_ckb_kicker = section.settings.kicker | strip
  assign crs_ckb_head = section.settings.heading | strip
  assign crs_ckb_sub = section.settings.subheading | strip
  assign crs_ckb_visual_type = section.settings.visual_type | default: 'image'
  assign crs_ckb_graph_kicker = section.settings.graph_kicker | strip
  assign crs_ckb_graph_note = section.settings.graph_note | strip
  if crs_ckb_kicker == '__none__'
    assign crs_ckb_kicker = ''
  endif
  assign crs_h_headline_lg = false
  if section.settings.heading_size == 'product_title'
    assign crs_h_headline_lg = true
  endif
-%}
{% style %}
  #section-{{ section.id }} {
    --crs-bg:       {{ section.settings.color_bg }};
    --crs-card-bg:  {{ section.settings.color_card_bg }};
    --crs-text:     {{ section.settings.color_text }};
    --crs-text-sec: {{ section.settings.color_text | color_modify: 'alpha', 0.6 }};
    --crs-text-ter: {{ section.settings.color_text | color_modify: 'alpha', 0.4 }};
    --crs-border:   {{ section.settings.color_text | color_modify: 'alpha', 0.05 }};
    padding-top:    {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
  }
{% endstyle %}

<div class="ckb-section" id="section-{{ section.id }}">
  <div class="ckb-container">
    <div class="ckb-card{% if section.settings.image_position == 'left' %} ckb-card--image-left{% endif %}">

      {%- comment -%} LEFT — text + testimonial {%- endcomment -%}
      <div class="ckb-content">
        <div class="ckb-text-block crs-heading-stack crs-heading-stack--align-{{ crs_h_align }}{% if crs_h_headline_lg %} crs-heading-stack--headline-lg{% endif %}">
          {%- if crs_ckb_kicker != blank -%}
            <p class="crs-heading-stack__kicker">{{ crs_ckb_kicker | escape }}</p>
          {%- endif -%}
          {%- if crs_ckb_head != blank or crs_ckb_sub != blank -%}
            <div class="crs-heading-stack__pair">
              {%- if crs_ckb_head != blank -%}
                <h2 class="crs-heading-stack__title">{{ crs_ckb_head | escape | newline_to_br }}</h2>
              {%- endif -%}
              {%- if crs_ckb_sub != blank -%}
                <p class="crs-heading-stack__subtitle">{{ crs_ckb_sub | escape | newline_to_br }}</p>
              {%- endif -%}
            </div>
          {%- endif -%}
        </div>

        {%- if section.settings.quote_text != blank -%}
          <figure class="ckb-quote">
            <blockquote>
              <p class="ckb-quote-body">{{ section.settings.quote_text | escape }}</p>
            </blockquote>
            <figcaption class="ckb-author">
              {%- if section.settings.author_image != blank -%}
                <div class="ckb-avatar">
                  {{
                    section.settings.author_image
                    | image_url: width: 84
                    | image_tag:
                      width: 42,
                      height: 42,
                      loading: 'lazy',
                      alt: section.settings.author_name | escape
                  }}
                </div>
              {%- endif -%}
              <div class="ckb-author-info">
                <span class="ckb-author-name">{{ section.settings.author_name | escape }}</span>
                <span class="ckb-author-role">{{ section.settings.author_role | escape }}</span>
              </div>
            </figcaption>
          </figure>
        {%- endif -%}
      </div>

      {%- comment -%} RIGHT — image {%- endcomment -%}
      <div class="ckb-visual{% if crs_ckb_visual_type == 'nad_graph' %} ckb-visual--graph{% endif %}">
        {%- if crs_ckb_visual_type == 'nad_graph' -%}
          <div class="ckb-nad" data-ckb-nad-graph>
            <div class="ckb-nad__frame">
              {%- if crs_ckb_graph_kicker != blank -%}
                <p class="ckb-nad__eyebrow">{{ crs_ckb_graph_kicker | escape }}</p>
              {%- endif -%}

              <div
                class="ckb-nad__chart"
                role="img"
                aria-labelledby="ckb-nad-title-{{ section.id }}"
                aria-describedby="ckb-nad-desc-{{ section.id }}"
              >
                <span class="visually-hidden" id="ckb-nad-title-{{ section.id }}">
                  Animierte Visualisierung eines altersbedingten NAD+ Rueckgangs
                </span>
                <span class="visually-hidden" id="ckb-nad-desc-{{ section.id }}">
                  Schematische Kurve mit hoehren NAD+ Werten in juengeren Jahren und niedrigeren Werten im hoeheren Alter.
                </span>

                <svg
                  class="ckb-nad__svg"
                  viewBox="0 0 720 520"
                  preserveAspectRatio="none"
                  aria-hidden="true"
                >
                  <defs>
                    <filter id="ckb-nad-shadow-{{ section.id }}" x="-20%" y="-20%" width="140%" height="140%">
                      <feDropShadow dx="0" dy="10" stdDeviation="10" flood-color="#111111" flood-opacity="0.08"></feDropShadow>
                    </filter>
                  </defs>

                  <g class="ckb-nad__grid">
                    <line x1="82" y1="76" x2="660" y2="76"></line>
                    <line x1="82" y1="164" x2="660" y2="164"></line>
                    <line x1="82" y1="252" x2="660" y2="252"></line>
                    <line x1="82" y1="340" x2="660" y2="340"></line>
                    <line x1="82" y1="428" x2="660" y2="428"></line>
                    <line x1="82" y1="428" x2="82" y2="58"></line>
                    <line x1="82" y1="428" x2="676" y2="428"></line>
                  </g>

                  <path
                    class="ckb-nad__line"
                    d="M 82 124 C 136 128, 180 138, 222 154 C 266 171, 308 194, 346 225 C 388 259, 432 291, 480 324 C 532 359, 586 385, 660 401"
                  ></path>

                  <g class="ckb-nad__markers" filter="url(#ckb-nad-shadow-{{ section.id }})">
                    <g class="ckb-nad__marker" style="--delay: 0.62s;">
                      <circle class="ckb-nad__marker-ripple" cx="176" cy="140" r="6"></circle>
                      <circle class="ckb-nad__marker-ring" cx="176" cy="140" r="6"></circle>
                      <circle class="ckb-nad__marker-core" cx="176" cy="140" r="2.5"></circle>
                    </g>
                    <g class="ckb-nad__marker" style="--delay: 1.38s;">
                      <circle class="ckb-nad__marker-ripple" cx="350" cy="228" r="5"></circle>
                      <circle class="ckb-nad__marker-ring" cx="350" cy="228" r="5"></circle>
                      <circle class="ckb-nad__marker-core" cx="350" cy="228" r="2.5"></circle>
                    </g>
                    <g class="ckb-nad__marker ckb-nad__marker--focus" style="--delay: 2.08s;">
                      <circle class="ckb-nad__marker-ripple" cx="610" cy="390" r="6"></circle>
                      <circle class="ckb-nad__marker-ring" cx="610" cy="390" r="6"></circle>
                      <circle class="ckb-nad__marker-core" cx="610" cy="390" r="2.5"></circle>
                    </g>
                  </g>
                </svg>

                <div class="ckb-nad__y-guide" aria-hidden="true">
                  <span class="ckb-nad__y-guide-label ckb-nad__y-guide-label--high">MEHR {{ section.settings.graph_axis_y | escape }}</span>
                  <span class="ckb-nad__y-guide-label ckb-nad__y-guide-label--low">WENIGER</span>
                </div>
                <div class="ckb-nad__x-guide" aria-hidden="true">
                  <span class="ckb-nad__x-label" style="--delay: 0.62s;">JUENGER</span>
                  <span class="ckb-nad__x-label" style="--delay: 1.38s;">{{ section.settings.graph_axis_x | upcase | escape }}</span>
                  <span class="ckb-nad__x-label" style="--delay: 2.08s;">AELTER</span>
                </div>
              </div>

              {%- if crs_ckb_graph_note != blank -%}
                <p class="ckb-nad__note">{{ crs_ckb_graph_note | escape }}</p>
              {%- endif -%}
            </div>
          </div>
        {%- elsif section.settings.image != blank -%}
          {{
            section.settings.image
            | image_url: width: 1400
            | image_tag:
              loading: 'lazy',
              class: 'ckb-visual-img',
              alt: section.settings.image.alt | escape
          }}
        {%- else -%}
          <div class="ckb-visual-placeholder">
            {{ 'image' | placeholder_svg_tag: 'ckb-placeholder-svg' }}
          </div>
        {%- endif -%}
      </div>

    </div>
  </div>
</div>

{% stylesheet %}
/* ===================================================
   Knowledge Base — crs-knowledge-base.liquid
   =================================================== */

.ckb-section {
  background: var(--crs-bg, #f7f7f4);
}

.ckb-container {
  max-width: 1280px;
  margin: 0 auto;
  padding: 0 20px;
}

/* Card — Text 1fr, Bild 2fr (Bild rechts: Text | Bild) */
.ckb-card {
  box-sizing: border-box;
  display: grid;
  grid-template-columns: 1fr 2fr;
  min-height: 560px;
  border-radius: 4px;
  overflow: hidden;
  background: var(--crs-card-bg, #f2f1ed);
  border: 1px solid var(--crs-border, rgba(38, 37, 30, 0.05));
}

/* Bild links: gleiche Proportionen, Spalten getauscht */
.ckb-card--image-left {
  grid-template-columns: 2fr 1fr;
}

.ckb-card--image-left .ckb-visual {
  grid-column: 1;
}

.ckb-card--image-left .ckb-content {
  grid-column: 2;
}

/* Left column */
.ckb-content {
  display: flex;
  flex-direction: column;
  justify-content: center;
  padding: 52px 44px;
  gap: 28px;
}

.ckb-text-block {
  display: flex;
  flex-direction: column;
}

/* Testimonial quote */
.ckb-quote {
  margin: 0;
  border-left: 2px solid var(--crs-border, rgba(38, 37, 30, 0.05));
  padding-left: 20px;
}

.ckb-quote-body {
  font-size: .9375rem;
  line-height: 1.6;
  color: var(--crs-text, #26251e);
  margin: 0 0 16px;
}

/* Author */
.ckb-author {
  display: flex;
  align-items: center;
  gap: 12px;
}

.ckb-avatar {
  width: 42px;
  height: 42px;
  border-radius: 50%;
  overflow: hidden;
  flex-shrink: 0;
  border: 1.5px solid var(--crs-border, rgba(38, 37, 30, 0.05));
}

.ckb-avatar img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

.ckb-author-info {
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.ckb-author-name {
  font-size: .875rem;
  font-weight: 600;
  color: var(--crs-text, #26251e);
}

.ckb-author-role {
  font-size: .8125rem;
  color: var(--crs-text-sec, rgba(38,37,30,0.6));
}

/* Right column — image */
.ckb-visual {
  position: relative;
  overflow: hidden;
}

.ckb-visual--graph {
  display: flex;
  align-items: stretch;
  padding: 0;
  background:
    linear-gradient(180deg, rgba(255, 255, 255, 0.08), transparent 45%),
    var(--crs-card-bg, #f2f1ed);
}

.ckb-visual-img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

.ckb-nad {
  position: relative;
  display: flex;
  width: 100%;
  min-height: 100%;
}

.ckb-nad__frame {
  position: relative;
  display: grid;
  grid-template-rows: auto 1fr auto;
  gap: 10px;
  width: 100%;
  padding: 32px 36px 28px;
  border: none;
  border-radius: 0;
  background: transparent;
  box-shadow: none;
  overflow: hidden;
}

.ckb-nad__frame::before {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(180deg, rgba(255, 255, 255, 0.22), transparent 32%);
  pointer-events: none;
}

.ckb-nad__eyebrow {
  margin: 0;
  position: relative;
  z-index: 1;
  font-size: 0.72rem;
  font-weight: 600;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--crs-text-ter, rgba(38, 37, 30, 0.4));
}

.ckb-nad__chart {
  position: relative;
  min-height: 392px;
}

.ckb-nad__svg {
  position: relative;
  z-index: 1;
  display: block;
  width: 100%;
  height: 100%;
}

.ckb-nad__grid line {
  stroke: #f5f5f5;
  stroke-width: 1;
  vector-effect: non-scaling-stroke;
}

.ckb-nad__line {
  fill: none;
  stroke: rgba(20, 20, 20, 0.92);
  stroke-width: 2;
  stroke-linecap: round;
  stroke-linejoin: round;
  opacity: 0;
}

.ckb-nad.is-visible .ckb-nad__line {
  opacity: 1;
  animation: ckb-nad-line-draw 2.5s cubic-bezier(0.45, 0, 0.2, 1) forwards;
}

.ckb-nad__marker {
  opacity: 0;
  transform-box: fill-box;
  transform-origin: center;
}

.ckb-nad.is-visible .ckb-nad__marker {
  animation: ckb-nad-marker-in 0.56s cubic-bezier(0.34, 1.56, 0.64, 1) forwards;
  animation-delay: var(--delay, 0s);
}

.ckb-nad__marker-ripple {
  fill: none;
  stroke: rgba(20, 20, 20, 0.16);
  stroke-width: 1.2;
  opacity: 0;
  transform-box: fill-box;
  transform-origin: center;
}

.ckb-nad__marker-ring {
  fill: rgba(255, 255, 255, 0.98);
  stroke: rgba(20, 20, 20, 0.18);
  stroke-width: 1.2;
}

.ckb-nad__marker-core {
  fill: rgba(20, 20, 20, 0.92);
}

.ckb-nad.is-visible .ckb-nad__marker-ripple {
  animation: ckb-nad-ripple 1.1s ease-out forwards;
  animation-delay: calc(var(--delay, 0s) + 0.08s);
}

.ckb-nad__y-guide,
.ckb-nad__x-guide {
  position: absolute;
  z-index: 2;
  pointer-events: none;
}

.ckb-nad__y-guide {
  inset-block: 30px 56px;
  inset-inline-start: 0;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
}

.ckb-nad__y-guide-label,
.ckb-nad__x-label {
  font-size: 0.7rem;
  font-family: var(--font-body);
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: rgba(20, 20, 20, 0.6);
  opacity: 0.22;
}

.ckb-nad__x-guide {
  inset-inline: 82px 44px;
  inset-block-end: 0;
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  align-items: center;
  gap: 12px;
}

.ckb-nad__x-guide::before {
  content: '';
  position: absolute;
  inset-inline: 32px 24px;
  inset-block-start: -14px;
  height: 1px;
  background: rgba(20, 20, 20, 0.08);
}

.ckb-nad__x-label {
  opacity: 0;
}

.ckb-nad__x-label:first-child {
  justify-self: start;
}

.ckb-nad__x-label:nth-child(2) {
  justify-self: center;
}

.ckb-nad__x-label:last-child {
  justify-self: end;
}

.ckb-nad.is-visible .ckb-nad__x-label {
  animation: ckb-nad-label-in 0.5s ease-out forwards;
  animation-delay: calc(var(--delay, 0s) + 0.04s);
}

.ckb-nad.is-complete .ckb-nad__chart {
  animation: ckb-nad-breathe 7s ease-in-out infinite;
}

.ckb-nad__note {
  position: relative;
  z-index: 1;
  margin: 0;
  max-width: 28ch;
  font-size: 0.8rem;
  line-height: 1.5;
  color: var(--crs-text-sec, rgba(38,37,30,0.6));
}

.ckb-visual-placeholder {
  width: 100%;
  height: 100%;
  min-height: 400px;
  background: var(--crs-card-bg, #f2f1ed);
  display: flex;
  align-items: center;
  justify-content: center;
}

.ckb-placeholder-svg {
  width: 60px;
  height: 60px;
  opacity: .3;
}

/* Responsive — tablet */
@media (max-width: 1024px) {
  .ckb-card {
    grid-template-columns: 1fr;
    grid-template-rows: auto 360px;
  }

  .ckb-card--image-left .ckb-visual,
  .ckb-card--image-left .ckb-content {
    grid-column: auto;
  }

  .ckb-visual {
    order: -1;
  }

  .ckb-visual--graph {
    min-height: 420px;
    padding: 0;
  }

  .ckb-content {
    padding: 40px 32px;
  }
}

/* Responsive — mobile */
@media (max-width: 640px) {
  .ckb-content {
    padding: 32px 20px;
    gap: 20px;
  }

  .ckb-visual {
    min-height: 280px;
  }

  .ckb-visual--graph {
    min-height: 360px;
    padding: 0;
  }

  .ckb-nad__frame {
    padding: 22px 18px 18px;
  }

  .ckb-nad__chart {
    min-height: 272px;
  }

  .ckb-nad__x-guide {
    inset-inline: 74px 18px;
    gap: 8px;
  }

  .ckb-nad__y-guide-label,
  .ckb-nad__x-label {
    font-size: 0.62rem;
  }
}
{% endstylesheet %}

{% javascript %}
  (function () {
    function setupGraph(graph) {
      if (!graph || graph.dataset.graphReady === 'true') return;

      var line = graph.querySelector('.ckb-nad__line');
      if (line) {
        var length = line.getTotalLength();
        line.style.strokeDasharray = length;
        line.style.strokeDashoffset = length;
        graph.style.setProperty('--ckb-nad-path-length', length);
      }

      graph.dataset.graphReady = 'true';
    }

    function observeGraph(graph) {
      if (!graph || graph.dataset.graphObserved === 'true') return;
      graph.dataset.graphObserved = 'true';

      function play() {
        if (graph.dataset.graphPlayed === 'true') return;
        graph.dataset.graphPlayed = 'true';
        graph.classList.add('is-visible');

        window.setTimeout(function () {
          graph.classList.add('is-complete');
        }, 2600);
      }

      if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
        play();
        graph.classList.remove('is-complete');
        return;
      }

      if (!('IntersectionObserver' in window)) {
        play();
        return;
      }

      var observer = new IntersectionObserver(function (entries) {
        entries.forEach(function (entry) {
          if (!entry.isIntersecting) return;
          play();
          observer.unobserve(entry.target);
        });
      }, {
        threshold: 0.18,
        rootMargin: '0px 0px -8% 0px'
      });

      observer.observe(graph);
    }

    function init(root) {
      var scope = root || document;
      var graphs = scope.querySelectorAll('[data-ckb-nad-graph]');

      graphs.forEach(function (graph) {
        setupGraph(graph);
        observeGraph(graph);
      });
    }

    document.addEventListener('shopify:section:load', function (event) {
      init(event.target);
    });

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', function () {
        init(document);
      });
    } else {
      init(document);
    }
  })();
{% endjavascript %}

{% style %}
  @keyframes ckb-nad-line-draw {
    from {
      stroke-dashoffset: var(--ckb-nad-path-length, 1);
    }
    to {
      stroke-dashoffset: 0;
    }
  }

  @keyframes ckb-nad-marker-in {
    from {
      opacity: 0;
      transform: scale(0);
    }
    72% {
      opacity: 1;
      transform: scale(1.1);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }

  @keyframes ckb-nad-ripple {
    0% {
      opacity: 0.42;
      transform: scale(1);
    }
    100% {
      opacity: 0;
      transform: scale(3.2);
    }
  }

  @keyframes ckb-nad-label-in {
    from {
      opacity: 0;
    }
    to {
      opacity: 0.6;
    }
  }

  @keyframes ckb-nad-breathe {
    0%,
    100% {
      opacity: 1;
    }
    50% {
      opacity: 0.94;
    }
  }
{% endstyle %}

{% schema %}
{
  "name": "Cursor Knowledge Base",
  "tag": "section",
  "class": "crs-knowledge-base",
  "settings": [
    {
      "type": "header",
      "content": "Farben"
    },
    {
      "type": "color",
      "id": "color_bg",
      "label": "Seiten-Hintergrund",
      "default": "#f7f7f4"
    },
    {
      "type": "color",
      "id": "color_card_bg",
      "label": "Karten-Hintergrund",
      "default": "#f2f1ed"
    },
    {
      "type": "color",
      "id": "color_text",
      "label": "Textfarbe",
      "default": "#26251e"
    },
    {
      "type": "header",
      "content": "Abstände"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Abstand oben",
      "min": 0,
      "max": 120,
      "step": 8,
      "unit": "px",
      "default": 64
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Abstand unten",
      "min": 0,
      "max": 120,
      "step": 8,
      "unit": "px",
      "default": 64
    },
    {
      "type": "header",
      "content": "Linke Spalte — Text"
    },
    {
      "type": "select",
      "id": "content_align",
      "label": "Ausrichtung (Überschrift)",
      "options": [
        { "value": "left", "label": "Links" },
        { "value": "center", "label": "Zentriert" },
        { "value": "right", "label": "Rechts" }
      ],
      "default": "left"
    },
    {
      "type": "select",
      "id": "heading_size",
      "label": "Überschrift Größe",
      "options": [
        { "value": "section", "label": "Standard (Abschnitt)" },
        { "value": "product_title", "label": "Wie Produkttitel" }
      ],
      "default": "section",
      "info": "Zeilenumbruch: Enter in Überschrift/Unterüberschrift."
    },
    {
      "type": "text",
      "id": "kicker",
      "label": "Kicker (optional)",
      "default": "__none__",
      "info": "__none__ = ausblenden (Theme-Editor)"
    },
    {
      "type": "textarea",
      "id": "heading",
      "label": "Überschrift",
      "default": "Deine Wissensdatenbank fürs Engineering"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Unterüberschrift",
      "default": "Ausgelegt für komplexe Codebasen, an denen Tausende von Entwicklern arbeiten."
    },
    {
      "type": "header",
      "content": "Testimonial"
    },
    {
      "type": "textarea",
      "id": "quote_text",
      "label": "Zitat",
      "default": "Über alle Rollen und Erfahrungsstufen hinweg sehen wir einen Anstieg des PR-Volumens um über 25 % und der durchschnittlichen PR-Größe um über 100 %."
    },
    {
      "type": "image_picker",
      "id": "author_image",
      "label": "Autorenfoto"
    },
    {
      "type": "text",
      "id": "author_name",
      "label": "Name",
      "default": "Anton Andreev"
    },
    {
      "type": "text",
      "id": "author_role",
      "label": "Position",
      "default": "Principal Software Engineer, Upwork"
    },
    {
      "type": "header",
      "content": "Rechte Spalte — Visual"
    },
    {
      "type": "select",
      "id": "visual_type",
      "label": "Visual-Typ",
      "options": [
        { "value": "image", "label": "Bild" },
        { "value": "nad_graph", "label": "Animierter NAD+ Graph" }
      ],
      "default": "image"
    },
    {
      "type": "select",
      "id": "image_position",
      "label": "Bildposition (Desktop)",
      "options": [
        { "value": "right", "label": "Rechts" },
        { "value": "left", "label": "Links" }
      ],
      "default": "right"
    },
    {
      "visible_if": "{{ section.settings.visual_type == 'nad_graph' }}",
      "type": "text",
      "id": "graph_kicker",
      "label": "Graph Kicker",
      "default": "Schematische Visualisierung"
    },
    {
      "visible_if": "{{ section.settings.visual_type == 'nad_graph' }}",
      "type": "text",
      "id": "graph_axis_x",
      "label": "Achse unten",
      "default": "Alter"
    },
    {
      "visible_if": "{{ section.settings.visual_type == 'nad_graph' }}",
      "type": "text",
      "id": "graph_axis_y",
      "label": "Achse links",
      "default": "NAD+"
    },
    {
      "visible_if": "{{ section.settings.visual_type == 'nad_graph' }}",
      "type": "textarea",
      "id": "graph_note",
      "label": "Graph Hinweis",
      "default": "Schematische Darstellung."
    },
    {
      "visible_if": "{{ section.settings.visual_type == 'image' }}",
      "type": "image_picker",
      "id": "image",
      "label": "Bild"
    }
  ],
  "blocks": [],
  "presets": [
    {
      "name": "Cursor Knowledge Base"
    }
  ]
}
{% endschema %}
