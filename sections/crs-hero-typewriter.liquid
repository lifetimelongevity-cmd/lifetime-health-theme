{% style %}
  #section-{{ section.id }} {
    --hero-text:     {{ section.settings.color_text | default: '#26251e' }};
    --hero-text-sec: {{ section.settings.color_text | default: '#26251e' | color_modify: 'alpha', 0.55 }};
    --hero-bg:       {{ section.settings.color_bg | default: '#f7f7f4' }};
    --hero-card-bg:  {{ section.settings.color_card_bg | default: '#f2f1ed' }};
    --hero-card-brd: {{ section.settings.color_text | default: '#26251e' | color_modify: 'alpha', 0.08 }};
    --hero-btn-r:    {{ settings.border_radius_buttons | default: 30 }}px;
    padding-top:    {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
  }
{% endstyle %}

<section
  id="section-{{ section.id }}"
  class="ltw-section{% if section.settings.right_type == 'none' %} ltw-section--no-visual{% endif %}"
>
  <div class="ltw-container">

    {%- comment -%} LEFT — text + CTA {%- endcomment -%}
    <div class="ltw-content">
      <div class="ltw-heading-group crs-heading-stack{% if section.settings.use_section_heading_size %} crs-heading-stack--headline-lg{% endif %} crs-heading-stack--homepage-truth">

        {%- if section.settings.eyebrow != blank -%}
          <p class="ltw-eyebrow">{{ section.settings.eyebrow | escape }}</p>
        {%- endif -%}

        <div class="crs-heading-stack__pair">
          <h1 class="ltw-heading crs-heading-stack__title" aria-label="{{ section.settings.headline | escape }}">
            {{ section.settings.headline | escape }}
          </h1>
          {%- if section.settings.subheading != blank -%}
            <p class="ltw-subheading crs-heading-stack__subtitle">{{ section.settings.subheading | escape }}</p>
          {%- endif -%}
        </div>
      </div>

      {%- if section.settings.cta_label != blank -%}
        <div class="ltw-cta">
          <a href="{{ section.settings.cta_url }}" class="ltw-btn">
            {{ section.settings.cta_label | escape }}
            {%- if section.settings.cta_icon != blank -%}
              <span class="ltw-btn__icon" aria-hidden="true">{{ section.settings.cta_icon }}</span>
            {%- endif -%}
          </a>
          {%- if section.settings.cta_secondary_label != blank -%}
            <a
              href="{{ section.settings.cta_secondary_anchor | default: '#' }}"
              class="ltw-btn ltw-btn--ghost"
            >
              {{ section.settings.cta_secondary_label | escape }}
            </a>
          {%- endif -%}
        </div>
      {%- endif -%}

    </div>

    {%- comment -%} RIGHT — image or dashboard card {%- endcomment -%}
    <div class="ltw-visual">
      {%- if section.settings.right_type == 'image' -%}
        {%- if section.settings.image != blank -%}
          {{
            section.settings.image
            | image_url: width: 1200
            | image_tag:
              loading: 'lazy',
              class: 'ltw-visual__img',
              alt: section.settings.image.alt | escape
          }}
        {%- else -%}
          <div class="ltw-visual__placeholder">{{ 'image' | placeholder_svg_tag: 'ltw-placeholder-svg' }}</div>
        {%- endif -%}

      {%- elsif section.settings.right_type == 'dashboard' -%}
        <div class="ltw-dash">

          {%- comment -%} Age card {%- endcomment -%}
          <div class="ltw-card ltw-card--age">
            <div class="ltw-card__label">{{ section.settings.dash_age_label | escape }}</div>
            <div
              class="ltw-card__age-num"
              data-ltw-age-number
              data-age-target="{{ section.settings.dash_bio_age | escape }}"
              data-age-start="{{ section.settings.dash_chron_age | escape }}"
            >
              {{ section.settings.dash_bio_age | escape }}
            </div>
            <div class="ltw-card__age-sub">{{ section.settings.dash_chron_label | escape }}: {{ section.settings.dash_chron_age | escape }}</div>
          </div>

          {%- comment -%} Metric cards {%- endcomment -%}
          <div class="ltw-metrics">
            {%- for block in section.blocks -%}
              {%- if block.type == 'metric' -%}
                {%- liquid
                  assign metric_detail = ''
                  case block.settings.metric_name
                    when 'Langlebigkeit'
                      assign metric_detail = 'Deine Genetik zeigt Merkmale, die mit verlangsamtem Altern verbunden sind – Gene wie FOXO3 unterstützen deine Langlebigkeit.'
                    when 'Entzündungsprofil'
                      assign metric_detail = 'Dein Entzündungsprofil zeigt Optimierungspotenzial – gezielte Maßnahmen können hier einen messbaren Unterschied machen.'
                    when 'Kognitive Gesundheit'
                      assign metric_detail = 'Deine epigenetischen Signale deuten auf eine solide kognitive Basis hin, die sich weiter stärken lässt.'
                  endcase
                -%}
                <div class="ltw-card ltw-card--metric ltw-metric-row" {{ block.shopify_attributes }}>
                  <button
                    type="button"
                    class="ltw-metric-row__button"
                    aria-expanded="false"
                  >
                    <span class="ltw-metric-row__top">
                      <span class="ltw-metric__name">{{ block.settings.metric_name | escape }}</span>
                      <span class="ltw-metric__chevron" aria-hidden="true">›</span>
                    </span>
                    <span class="ltw-bar">
                      <span class="ltw-bar__fill" style="--w: {{ block.settings.metric_value }}%; background: {{ block.settings.metric_color }};"></span>
                    </span>
                    <span class="ltw-metric-row__detail-wrap">
                      <span class="ltw-metric-row__detail">{{ metric_detail }}</span>
                    </span>
                  </button>
                </div>
              {%- endif -%}
            {%- endfor -%}
          </div>

        </div>
      {%- endif -%}
    </div>

  </div>
</section>

<script>
(function () {
  var root = document.getElementById('section-{{ section.id }}');
  if (!root) return;

  function animateAgeNumber() {
    var ageNumber = root.querySelector('[data-ltw-age-number]');
    if (!ageNumber) return;

    var reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    var target = parseFloat(ageNumber.dataset.ageTarget);
    var startCandidate = parseFloat(ageNumber.dataset.ageStart);

    if (Number.isNaN(target)) return;

    var start = Number.isNaN(startCandidate) ? target + 6 : startCandidate;
    if (start <= target) {
      start = target + 4;
    }

    function render(value) {
      var hasFraction = String(ageNumber.dataset.ageTarget || '').indexOf('.') !== -1;
      ageNumber.textContent = hasFraction ? value.toFixed(1) : String(Math.round(value));
    }

    if (reducedMotion) {
      render(target);
      return;
    }

    var duration = 1600;
    var startTime = null;

    function step(timestamp) {
      if (startTime === null) startTime = timestamp;
      var progress = Math.min((timestamp - startTime) / duration, 1);
      var eased = 1 - Math.pow(1 - progress, 3);
      var current = start + (target - start) * eased;
      render(current);

      if (progress < 1) {
        window.requestAnimationFrame(step);
      }
    }

    render(start);
    window.requestAnimationFrame(step);
  }

  function startAgeAnimationWhenVisible() {
    var ageNumber = root.querySelector('[data-ltw-age-number]');
    if (!ageNumber || ageNumber.dataset.ageAnimated === 'true') return;

    function startAnimation() {
      if (ageNumber.dataset.ageAnimated === 'true') return;
      ageNumber.dataset.ageAnimated = 'true';
      animateAgeNumber();
    }

    if (!('IntersectionObserver' in window)) {
      startAnimation();
      return;
    }

    var observer = new IntersectionObserver(function(entries) {
      entries.forEach(function(entry) {
        if (!entry.isIntersecting) return;
        startAnimation();
        observer.disconnect();
      });
    }, { threshold: 0.45 });

    observer.observe(ageNumber);
  }

  /* Animate dashboard bars on load with a light stagger */
  root.querySelectorAll('.ltw-bar__fill').forEach(function(el, index) {
    el.style.width = '0';
    setTimeout(function() {
      el.style.transition = 'width 800ms ease-out';
      el.style.width = el.style.getPropertyValue('--w') || el.dataset.w;
    }, index * 150);
  });

  var metricRows = root.querySelectorAll('.ltw-metric-row');

  function closeMetricRow(row) {
    var button = row.querySelector('.ltw-metric-row__button');
    var detailWrap = row.querySelector('.ltw-metric-row__detail-wrap');

    row.classList.remove('is-open');
    if (button) {
      button.setAttribute('aria-expanded', 'false');
    }
    if (detailWrap) {
      detailWrap.style.maxHeight = '0px';
    }
  }

  function openMetricRow(row) {
    var button = row.querySelector('.ltw-metric-row__button');
    var detailWrap = row.querySelector('.ltw-metric-row__detail-wrap');

    metricRows.forEach(function(item) {
      if (item !== row) {
        closeMetricRow(item);
      }
    });

    row.classList.add('is-open');
    if (button) {
      button.setAttribute('aria-expanded', 'true');
    }
    if (detailWrap) {
      detailWrap.style.maxHeight = detailWrap.scrollHeight + 'px';
    }
  }

  metricRows.forEach(function(row) {
    var button = row.querySelector('.ltw-metric-row__button');
    var detailWrap = row.querySelector('.ltw-metric-row__detail-wrap');

    if (detailWrap) {
      detailWrap.style.maxHeight = '0px';
    }

    if (!button) return;

    button.addEventListener('click', function() {
      if (row.classList.contains('is-open')) {
        closeMetricRow(row);
      } else {
        openMetricRow(row);
      }
    });
  });

  startAgeAnimationWhenVisible();
})();
</script>

{% stylesheet %}
/* ===================================================
   Hero Typewriter — lt-hero-typewriter.liquid
   =================================================== */

.ltw-section {
  background: var(--hero-bg, #efede6);
  overflow: hidden;
}

/* Two-column layout */
.ltw-container {
  max-width: 1280px;
  margin: 0 auto;
  padding: 0 40px;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 64px;
  align-items: center;
}

/* Text-only hero (e.g. collection intro): one column, readable measure */
.ltw-section--no-visual .ltw-container {
  grid-template-columns: 1fr;
  gap: 0;
}

.ltw-section--no-visual .ltw-visual {
  display: none;
}

.ltw-section--no-visual .ltw-content {
  max-width: min(65ch, 100%);
}

/* Left column */
.ltw-content {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
}

/* Eyebrow */
.ltw-eyebrow {
  font-family: var(--font-body);
  font-size: var(--text-micro);
  font-weight: var(--weight-medium);
  line-height: var(--leading-tight);
  letter-spacing: .12em;
  text-transform: uppercase;
  color: var(--hero-text-sec, rgba(22,21,20,.45));
  margin: 0 0 8px;
}

/* Headline */
.ltw-heading {
  color: var(--hero-text, #161514);
  margin: 0;
}

.ltw-heading-group {
  margin-bottom: 32px;
}

/* Subheading */
.ltw-subheading {
  color: var(--hero-text-sec, rgba(22,21,20,.72));
  margin: 0;
}

.ltw-heading-group .crs-heading-stack__pair {
  gap: 0;
}

.ltw-heading-group .ltw-subheading.crs-heading-stack__subtitle {
  margin-top: 0;
}

.ltw-heading-group.crs-heading-stack .ltw-heading.crs-heading-stack__title {
  color: var(--hero-text, #161514);
}

.ltw-heading-group.crs-heading-stack .ltw-subheading.crs-heading-stack__subtitle {
  color: var(--hero-text-sec, rgba(22,21,20,.72));
}

/* CTA */
.ltw-cta {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
}

.ltw-btn {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background: var(--color-dark);
  color: var(--color-text-on-dark);
  font-family: var(--font-body);
  font-size: var(--text-body-sm);
  font-weight: var(--weight-medium);
  line-height: 1;
  padding: 14px 22px;
  border-radius: var(--hero-btn-r, 30px);
  text-decoration: none;
  transition: opacity .15s ease;
}

.ltw-btn:hover { opacity: .82; }

.ltw-btn--ghost {
  background: transparent;
  color: var(--hero-text, #161514);
  border: 1.5px solid currentColor;
  opacity: 0.55;
}

.ltw-btn--ghost:hover {
  opacity: 0.9;
}

.ltw-btn__icon {
  font-size: 1rem;
  line-height: 1;
}

/* ---- Right column ---- */
.ltw-visual {
  display: flex;
  align-items: center;
  justify-content: center;
}

/* Image variant */
.ltw-visual__img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  border-radius: 4px;
  display: block;
}

.ltw-visual__placeholder {
  width: 100%;
  aspect-ratio: 4 / 3;
  background: var(--hero-card-bg, #f2f1ed);
  border-radius: 4px;
  display: flex;
  align-items: center;
  justify-content: center;
  border: 1px solid var(--hero-card-brd, rgba(38,37,30,.08));
}

.ltw-placeholder-svg {
  width: 60px;
  height: 60px;
  opacity: .25;
}

/* Dashboard variant */
.ltw-dash {
  width: 100%;
  max-width: 360px;
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.ltw-card {
  background: var(--color-surface-primary);
  border-radius: var(--radius-xl);
  box-shadow: 0 1px 4px rgba(0,0,0,.06);
  border: 0;
  padding: 20px 24px;
}

/* Age card */
.ltw-card--age {
  text-align: center;
  padding: 28px 24px;
}

.ltw-card__label {
  font-family: var(--font-body);
  font-size: var(--text-micro);
  font-weight: var(--weight-medium);
  letter-spacing: .12em;
  text-transform: uppercase;
  opacity: .48;
  margin-bottom: 8px;
  color: var(--hero-text, #26251e);
}

.ltw-card__age-num {
  font-family: var(--font-heading);
  font-size: clamp(3rem, 6vw, 4rem);
  font-weight: var(--weight-regular);
  line-height: 1;
  letter-spacing: -.04em;
  color: var(--hero-text, #26251e);
  margin-bottom: 6px;
}

.ltw-card__age-sub {
  font-family: var(--font-body);
  font-size: var(--text-body-sm);
  font-weight: var(--weight-regular);
  opacity: .55;
  color: var(--hero-text, #26251e);
}

/* Metric cards */
.ltw-metrics {
  display: flex;
  flex-direction: column;
  width: 100%;
  align-items: stretch;
  gap: 12px;
}

.ltw-metric-row {
  width: 100%;
  max-width: none;
  box-sizing: border-box;
  padding: 16px 18px;
}

.ltw-metric-row__button {
  display: block;
  width: 100%;
  padding: 0;
  border: 0;
  background: transparent;
  color: inherit;
  text-align: left;
  cursor: pointer;
  -webkit-appearance: none;
}

.ltw-metric-row__top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  margin-bottom: 10px;
}

.ltw-metric__name {
  display: block;
  font-family: var(--font-body);
  font-size: .8125rem;
  font-weight: var(--weight-medium);
  color: #6b6b63;
  margin: 0;
  opacity: 1;
}

.ltw-metric__chevron {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  font-family: var(--font-body);
  font-size: .95rem;
  line-height: 1;
  color: #6b6b63;
  transform: rotate(0deg);
  transition: transform 200ms ease-out;
}

.ltw-metric-row.is-open .ltw-metric__chevron {
  transform: rotate(90deg);
}

.ltw-bar {
  display: block;
  width: 100%;
  height: 6px;
  background: #e8e8e4;
  border-radius: 999px;
  overflow: hidden;
}

.ltw-bar__fill {
  display: block;
  height: 100%;
  border-radius: 999px;
  width: 0;
}

.ltw-metric-row__detail-wrap {
  display: block;
  max-height: 0;
  overflow: hidden;
  transition: max-height 250ms ease-out;
}

.ltw-metric-row__detail {
  display: block;
  margin: 10px 0 0;
  font-family: var(--font-body);
  font-size: 13px;
  font-weight: var(--weight-regular);
  line-height: 1.5;
  color: #6b6b63;
}

/* Responsive */
@media (max-width: 1024px) {
  .ltw-container {
    grid-template-columns: 1fr;
    gap: 48px;
    padding: 0 24px;
  }
}

@media (max-width: 640px) {
  .ltw-container {
    padding: 0 20px;
    gap: 36px;
  }

  .ltw-heading-group {
    margin-bottom: 24px;
  }
}
{% endstylesheet %}

{% schema %}
{
  "name": "Hero Typewriter",
  "tag": "section",
  "class": "lt-hero-typewriter",
  "settings": [
    {
      "type": "header",
      "content": "Abstände"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Abstand oben",
      "min": 0,
      "max": 200,
      "step": 8,
      "default": 120,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Abstand unten",
      "min": 0,
      "max": 200,
      "step": 8,
      "default": 120,
      "unit": "px"
    },
    {
      "type": "header",
      "content": "Farben"
    },
    {
      "type": "color",
      "id": "color_bg",
      "label": "Hintergrundfarbe"
    },
    {
      "type": "color",
      "id": "color_card_bg",
      "label": "Kartenhintergrund"
    },
    {
      "type": "color",
      "id": "color_text",
      "label": "Textfarbe"
    },
    {
      "type": "header",
      "content": "Inhalt — Links"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "Tab"
    },
    {
      "type": "text",
      "id": "headline",
      "label": "Headline",
      "default": "Dein Körper altert anders als du denkst."
    },
    {
      "type": "checkbox",
      "id": "use_section_heading_size",
      "label": "Section-Heading-Größe verwenden",
      "default": true
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Subheading",
      "default": "Tab anticipates where you're headed and suggests changes."
    },
    {
      "type": "text",
      "id": "cta_label",
      "label": "Button Text",
      "default": "Jetzt entdecken"
    },
    {
      "type": "url",
      "id": "cta_url",
      "label": "Button Link"
    },
    {
      "type": "text",
      "id": "cta_icon",
      "label": "Button Icon",
      "default": "↓",
      "info": "z.B. → ↓ ⤓"
    },
    {
      "type": "text",
      "id": "cta_secondary_label",
      "label": "Sekundärer CTA Text",
      "info": "z.B. Wie es funktioniert ↓ — Ghost-Button neben primärem CTA. Leer lassen = ausblenden."
    },
    {
      "type": "text",
      "id": "cta_secondary_anchor",
      "label": "Sekundärer CTA Anker",
      "info": "Anchor-Link, z.B. #shopify-section-loop"
    },
    {
      "type": "checkbox",
      "id": "show_trust_ticker",
      "label": "Trust-Ticker-Hinweis",
      "default": false,
      "info": "Nur als Referenz — der Ticker ist eine eigenständige Section (lt-hp-trust-ticker) in der Sektionsreihenfolge."
    },
    {
      "type": "header",
      "content": "Rechte Seite"
    },
    {
      "type": "select",
      "id": "right_type",
      "label": "Inhalt rechts",
      "options": [
        { "value": "none",      "label": "Nichts" },
        { "value": "image",     "label": "Bild" },
        { "value": "dashboard", "label": "Dashboard-Karte" }
      ],
      "default": "dashboard"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Bild",
      "info": "Wird angezeigt wenn \"Bild\" ausgewählt ist"
    },
    {
      "type": "header",
      "content": "Dashboard — Alter"
    },
    {
      "type": "text",
      "id": "dash_age_label",
      "label": "Label Biologisches Alter",
      "default": "Dein Biologisches Alter"
    },
    {
      "type": "text",
      "id": "dash_bio_age",
      "label": "Biologisches Alter",
      "default": "37"
    },
    {
      "type": "text",
      "id": "dash_chron_label",
      "label": "Label Chronologisches Alter",
      "default": "Chronologisches Alter"
    },
    {
      "type": "text",
      "id": "dash_chron_age",
      "label": "Chronologisches Alter",
      "default": "44"
    }
  ],
  "blocks": [
    {
      "type": "metric",
      "name": "Metrik",
      "settings": [
        {
          "type": "text",
          "id": "metric_name",
          "label": "Name",
          "default": "Langlebigkeit"
        },
        {
          "type": "range",
          "id": "metric_value",
          "label": "Wert",
          "min": 0,
          "max": 100,
          "step": 1,
          "default": 85
        },
        {
          "type": "color",
          "id": "metric_color",
          "label": "Farbe",
          "default": "#65c0b6"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Hero Typewriter",
      "blocks": [
        { "type": "metric", "settings": { "metric_name": "Langlebigkeit", "metric_value": 85, "metric_color": "#65c0b6" } },
        { "type": "metric", "settings": { "metric_name": "Entzündungsprofil", "metric_value": 34, "metric_color": "#f4b740" } },
        { "type": "metric", "settings": { "metric_name": "Kognitive Degeneration", "metric_value": 72, "metric_color": "#f4b740" } }
      ]
    }
  ]
}
{% endschema %}
