{%- liquid
  assign crs_h_align = section.settings.content_align | default: 'left'
  if crs_h_align != 'left' and crs_h_align != 'center' and crs_h_align != 'right'
    assign crs_h_align = 'left'
  endif
  assign crs_mg_kicker = section.settings.kicker | strip
  assign crs_mg_head = section.settings.heading | strip
  assign crs_mg_sub = section.settings.subheading | strip
  if crs_mg_kicker == '__none__'
    assign crs_mg_kicker = ''
  endif
  if crs_mg_head == '__none__'
    assign crs_mg_head = ''
  endif
  if crs_mg_sub == '__none__'
    assign crs_mg_sub = ''
  endif
  # Fliesstext gehoert NICHT in crs-heading-stack__pair (docs/section-heading-stack.md).
  # Laengere Einordnungen laufen ueber `intro` unterhalb des Heading-Stacks.
  assign crs_mg_intro = section.settings.intro | strip
  if crs_mg_intro == '__none__'
    assign crs_mg_intro = ''
  endif
  assign crs_h_headline_lg = false
  if section.settings.heading_size == 'product_title'
    assign crs_h_headline_lg = true
  endif
  assign crs_h_headline_md = false
  if section.settings.heading_size == 'feature'
    assign crs_h_headline_md = true
  endif
  # Zweispaltig ist Opt-in: dieselbe Section laeuft auch auf der AGE&DNA-PDP,
  # wo die volle Breite ueber drei Kennzahlen richtig bleibt.
  assign crs_mg_split = false
  if section.settings.layout_variant == 'split'
    assign crs_mg_split = true
  endif
-%}
{% style %}
  #section-{{ section.id }} {
    --crs-bg:       {{ section.settings.color_bg }};
    --crs-card-bg:  {{ section.settings.color_card_bg }};
    --crs-text:     {{ section.settings.color_text }};
    --crs-text-sec: {{ section.settings.color_text | color_modify: 'alpha', 0.72 }};
    --crs-text-ter: {{ section.settings.color_text | color_modify: 'alpha', 0.66 }};
    --crs-border:   {{ section.settings.color_text | color_modify: 'alpha', 0.05 }};
    padding-top:    min({{ section.settings.padding_top }}px, 15vw);
    padding-bottom: min({{ section.settings.padding_bottom }}px, 15vw);
  }
{% endstyle %}

<div class="mg-section" id="section-{{ section.id }}">
  <div class="mg-container">
   <div class="mg-layout{% if crs_mg_split %} mg-layout--split{% endif %}">
    <div class="mg-aside">

    {%- if crs_mg_kicker != blank or crs_mg_head != blank or crs_mg_sub != blank -%}
      <div class="mg-header{% if crs_mg_intro != blank %} mg-header--with-intro{% endif %} crs-heading-stack crs-heading-stack--align-{{ crs_h_align }}{% if crs_h_headline_lg %} crs-heading-stack--headline-lg{% elsif crs_h_headline_md %} crs-heading-stack--headline-md{% endif %}">
        {%- if crs_mg_kicker != blank -%}
          <p class="crs-heading-stack__kicker">{{ crs_mg_kicker | escape }}</p>
        {%- endif -%}
        {%- if crs_mg_head != blank or crs_mg_sub != blank -%}
          <div class="crs-heading-stack__pair">
            {%- if crs_mg_head != blank -%}
              <h2 class="crs-heading-stack__title">{{ crs_mg_head | escape | newline_to_br }}</h2>
            {%- endif -%}
            {%- if crs_mg_sub != blank -%}
              <p class="crs-heading-stack__subtitle">{{ crs_mg_sub | escape | newline_to_br }}</p>
            {%- endif -%}
          </div>
        {%- endif -%}
      </div>
    {%- endif -%}

    {%- if crs_mg_intro != blank -%}
      <p class="mg-intro">{{ crs_mg_intro | escape | newline_to_br }}</p>
    {%- endif -%}

    </div>
    <div class="mg-main">

    {%- if section.blocks.size > 0 -%}
      <div class="mg-grid" data-count="{{ section.blocks.size }}">
        {%- for block in section.blocks -%}
          <div class="mg-card{% if block.settings.source != blank %} mg-card--with-source{% endif %}" {{ block.shopify_attributes }}>
            <div class="mg-number">{{ block.settings.number | escape }}</div>
            {%- if block.settings.description != blank or block.settings.source != blank -%}
              <div class="mg-text">
                {%- if block.settings.description != blank -%}
                  <div class="mg-desc">{{ block.settings.description | escape }}</div>
                {%- endif -%}
                {%- if block.settings.source != blank -%}
                  <div class="mg-source">{{ block.settings.source | escape }}</div>
                {%- endif -%}
              </div>
            {%- endif -%}
          </div>
        {%- endfor -%}
      </div>
    {%- endif -%}

    {%- if section.settings.note != blank -%}
      <p class="mg-note">{{ section.settings.note }}</p>
    {%- endif -%}

    </div>
   </div>
  </div>
</div>

{% stylesheet %}
/* ===================================================
   Metrics Row — crs-metrics-row.liquid
   =================================================== */

.mg-section {
  background: var(--crs-bg, #f7f7f4);
}

.mg-container {
  max-width: 1280px;
  margin: 0 auto;
  padding: 0 20px;
}

/* Grundzustand einspaltig: reine Bloecke, die Abstaende kommen weiter aus
   .mg-header / .mg-intro. Zweispaltig nur per Modifier. */
.mg-layout {
  display: block;
}

@media (min-width: 980px) {
  .mg-layout--split {
    display: grid;
    grid-template-columns: minmax(260px, 380px) minmax(0, 1fr);
    gap: var(--space-7);
    align-items: start;
  }

  .mg-layout--split .mg-header,
  .mg-layout--split .mg-header--with-intro {
    margin-bottom: 0;
  }

  .mg-layout--split .mg-header--with-intro {
    margin-bottom: var(--space-4);
  }

  .mg-layout--split .mg-intro {
    margin-bottom: 0;
    max-width: none;
  }

}

/* Die Kennzahlen stapeln in der Split-Variante immer: drei in zwei Spalten
   lassen unter 980px eine Waise in Zeile zwei stehen, und in der rechten
   Spalte waere jede von drei Spalten so schmal, dass ein Wert wie
   "bis −100 %" in Zahlengroesse umbricht. Nur fuer die Split-Variante, damit
   die AGE&DNA-PDP mit ihrem eigenen Kennzahlen-Satz unberuehrt bleibt.
   [data-count] steht hier nur wegen der Spezifitaet: (0,3,0) schlaegt die
   Ein- und Zwei-Zahlen-Raster unten (0,2,0), die sonst spaeter im Quelltext
   gewinnen wuerden. Das Attribut sitzt immer am Grid, matcht also immer. */
.mg-layout--split .mg-grid[data-count] {
  grid-template-columns: minmax(0, 1fr);
  gap: var(--space-3);
}

/* Erst ab 1080px Zahl links, Beschreibung rechts. Bei 980px blieben neben der
   380px-Textspalte und der 18rem-Zahlenspalte nur rund 150px fuer die
   Beschreibung, die dann fuenfzeilig neben einer 52px-Zahl lief. Darunter
   bleiben die Karten in ihrer normalen Form, Zahl ueber Text.
   (0,2,0) schlaegt .mg-card--with-source (0,1,0). */
@media (min-width: 1080px) {
  .mg-layout--split .mg-card {
    display: grid;
    grid-template-columns: minmax(0, 18rem) minmax(0, 1fr);
    gap: var(--space-5);
    align-items: center;
    aspect-ratio: auto;
    min-height: 0;
    padding: var(--space-5);
  }

  .mg-layout--split .mg-text {
    margin-top: 0;
  }
}

/* Header */
.mg-header {
  margin-bottom: var(--space-7);
}

.mg-header--with-intro {
  margin-bottom: var(--space-4);
}

/* Section-Description: bewusst ausserhalb des Heading-Stacks, damit Titel und
   Subline ein Block bleiben und Fliesstext nicht auf Headline-Groesse laeuft. */
.mg-intro {
  font-family: var(--font-body);
  font-size: var(--text-body);
  font-weight: var(--weight-regular);
  line-height: var(--leading-relaxed);
  color: var(--crs-text-sec, rgba(38, 37, 30, 0.72));
  max-width: 62ch;
  margin: 0 0 var(--space-7);
}

/* Grid */
.mg-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 10px;
}

/* Drei belegbare Zahlen sind der Ausnahmefall, nicht die Regel: die meisten
   Supplements tragen eine oder zwei Zahlen mit Quellenzeile. Ein hartes
   Dreierraster liesse dort Luecken stehen. Die Karten behalten in allen drei
   Faellen dieselbe Breite, damit eine Zwei-Zahlen-Reihe nicht wie ein
   aufgeblasenes Dreierraster aussieht, sondern wie eine kurze Reihe.
   Nur ab 1025px: darunter uebernehmen die Breakpoints weiter unten. */
@media (min-width: 1025px) {
  .mg-grid[data-count="1"] {
    grid-template-columns: minmax(0, 26rem);
  }

  .mg-grid[data-count="2"] {
    grid-template-columns: repeat(2, minmax(0, 26rem));
  }
}

/* Card */
.mg-card {
  box-sizing: border-box;
  background: var(--crs-card-bg, #f2f1ed);
  border: 1px solid var(--crs-border, rgba(38, 37, 30, 0.05));
  border-radius: 4px;
  padding: 12.5px 15px 15px;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  aspect-ratio: 16 / 9;
}

/* Big number */
.mg-number {
  font-family: var(--font-heading);
  font-size: 52px;
  font-weight: 400;
  line-height: 59.8px;
  letter-spacing: -1.3px;
  color: var(--crs-text, #26251e);
}

/* Text block (pushed to the bottom of the card) */
.mg-text {
  margin-top: auto;
}

/* Description */
.mg-desc {
  font-size: 16px;
  font-weight: 400;
  line-height: 1.5;
  color: var(--crs-text, #26251e);
}

/* Source / method line */
.mg-source {
  font-size: 13px;
  font-weight: 400;
  line-height: 1.5;
  color: var(--crs-text-sec, rgba(38, 37, 30, 0.72));
  margin-top: var(--space-2);
}

/* Cards carrying a source line need room to breathe */
.mg-card--with-source {
  aspect-ratio: auto;
  min-height: 220px;
}

/* Note below the grid */
.mg-note {
  font-size: 14px;
  line-height: 1.5;
  color: var(--crs-text-sec, rgba(38, 37, 30, 0.72));
  margin: var(--space-5) 0 0;
}

.mg-note a {
  color: var(--color-teal);
  text-decoration: underline;
  text-underline-offset: 3px;
  text-decoration-color: var(--color-gray-400);
  transition: color 0.15s ease, text-decoration-color 0.15s ease;
}

.mg-note a:hover {
  color: var(--color-teal-dark);
  text-decoration-color: var(--color-teal-dark);
}

/* Tablet — 2 columns */
@media (max-width: 1024px) {
  .mg-grid {
    grid-template-columns: repeat(2, 1fr);
  }

  .mg-card {
    aspect-ratio: auto;
    min-height: 140px;
  }
}

/* Mobile — 1 column */
@media (max-width: 640px) {
  .mg-grid {
    grid-template-columns: 1fr;
  }

  .mg-number {
    font-size: 40px;
    line-height: 1.2;
    letter-spacing: -1px;
  }

  .mg-card {
    min-height: 120px;
  }
}
{% endstylesheet %}

{% schema %}
{
  "name": "Metrics Row",
  "tag": "section",
  "class": "crs-metrics-row",
  "settings": [
    {
      "type": "header",
      "content": "Farben"
    },
    {
      "type": "color",
      "id": "color_bg",
      "label": "Seiten-Hintergrund",
      "default": "#f7f7f4"
    },
    {
      "type": "color",
      "id": "color_card_bg",
      "label": "Karten-Hintergrund",
      "default": "#f2f1ed"
    },
    {
      "type": "color",
      "id": "color_text",
      "label": "Textfarbe",
      "default": "#26251e"
    },
    {
      "type": "header",
      "content": "Abstände"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Abstand oben",
      "min": 0,
      "max": 120,
      "step": 8,
      "unit": "px",
      "default": 80
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Abstand unten",
      "min": 0,
      "max": 120,
      "step": 8,
      "unit": "px",
      "default": 80
    },
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "select",
      "id": "layout_variant",
      "label": "Aufbau",
      "options": [
        { "value": "stacked", "label": "Untereinander (Überschrift über den Kennzahlen)" },
        { "value": "split", "label": "Zweispaltig (Überschrift links, Kennzahlen rechts)" }
      ],
      "default": "stacked",
      "info": "Zweispaltig greift ab 980px und stapelt die Kennzahlen mit der Zahl links. Darunter läuft die Section immer untereinander."
    },
    {
      "type": "header",
      "content": "Inhalt"
    },
    {
      "type": "select",
      "id": "content_align",
      "label": "Ausrichtung (Überschrift)",
      "options": [
        { "value": "left", "label": "Links" },
        { "value": "center", "label": "Zentriert" },
        { "value": "right", "label": "Rechts" }
      ],
      "default": "left"
    },
    {
      "type": "select",
      "id": "heading_size",
      "label": "Überschrift Größe",
      "options": [
        { "value": "section", "label": "Standard (Abschnitt)" },
        { "value": "feature", "label": "Mittel (Section-Headline)" },
        { "value": "product_title", "label": "Wie Produkttitel" }
      ],
      "default": "section",
      "info": "Zeilenumbruch: Enter in Überschrift/Unterüberschrift."
    },
    {
      "type": "text",
      "id": "kicker",
      "label": "Kicker (optional)",
      "default": "__none__",
      "info": "__none__ = ausblenden (Theme-Editor)"
    },
    {
      "type": "textarea",
      "id": "heading",
      "label": "Überschrift (optional)",
      "default": "__none__",
      "info": "__none__ = ausblenden (Theme-Editor)"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Unterüberschrift (optional)",
      "default": "__none__",
      "info": "__none__ = ausblenden (Theme-Editor). Kurz halten, läuft auf Headline-Größe."
    },
    {
      "type": "textarea",
      "id": "intro",
      "label": "Einleitungstext (optional)",
      "default": "__none__",
      "info": "__none__ = ausblenden. Für längere Einordnungen statt Unterüberschrift."
    },
    {
      "type": "inline_richtext",
      "id": "note",
      "label": "Fußnote unter den Kennzahlen (optional)",
      "info": "Für Quellenangabe oder fachliche Zeichnung. Leer lassen = ausblenden."
    }
  ],
  "blocks": [
    {
      "type": "metric",
      "name": "Kennzahl",
      "settings": [
        {
          "type": "text",
          "id": "number",
          "label": "Zahl / Wert",
          "default": ">99,9%"
        },
        {
          "type": "textarea",
          "id": "description",
          "label": "Beschreibung",
          "default": "bestätigte NMN-Reinheit"
        },
        {
          "type": "text",
          "id": "source",
          "label": "Quelle / Methode (optional)",
          "info": "Kleine Zeile unter der Beschreibung, z. B. Labor und Messmethode."
        }
      ]
    }
  ],
  "max_blocks": 3,
  "presets": [
    {
      "name": "Metrics Row",
      "blocks": [
        {
          "type": "metric",
          "settings": {
            "number": "NAD⁺ ×2",
            "description": "Verdopplung der NAD⁺-Werte im Blut nach 12 Wochen – belegt durch Humanstudien"
          }
        },
        {
          "type": "metric",
          "settings": {
            "number": "+11 %",
            "description": "mehr körperliche Leistung im 6-Minuten-Gehtest nach 60 Tagen – randomisierte Placebostudie"
          }
        },
        {
          "type": "metric",
          "settings": {
            "number": "– 50 %",
            "description": "so stark sinken NAD⁺-Werte zwischen 20 und 60 Jahren – der Mechanismus hinter NMN"
          }
        }
      ]
    }
  ]
}
{% endschema %}
