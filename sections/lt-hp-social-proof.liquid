{% comment %}
  lt-hp-social-proof — Homepage Social Proof + Presse (Section 5)
  ───────────────────────────────────────────────────────────────
  §12 Build Spec:
  - Surface:          --color-surface-primary (white — direkter Kontrast nach warmem Product Hero)
  - Dominant element: Aggregate-Stat ("87 %") — zentrales Skalierungs-Signal nach den Testimonials
  - Heading size:     --text-h1 (36px) · kein Karussell — statische 3-col Cards
  - Content pattern:  testimonial-card × 3 + Stat-Banner + Press-Logo-Zeile
  - CTA:              keiner — Energie geht direkt in Section 6 über (Wissenschaft)
  - Removed:          Sterne (das sind Ergebnis-Testimonials, keine Produkt-Bewertungen),
                      Card-CTAs, Datums-Feld (kein Relevanz-Signal hier)
  - Quality move:     result_metric als Teal-Pill in der Card — das konkrete Messergebnis
                      ("Biologisches Alter: 37 · Chronologisch: 44") ist der stärkste
                      Conversion-Treiber: es nimmt das eigene Ergebnis vorweg.
{% endcomment %}

{%- liquid
  assign hp_headline_lg = false
  if section.settings.heading_size == 'product_title'
    assign hp_headline_lg = true
  endif
-%}

{% style %}
  #shopify-section-{{ section.id }} {
    {%- if section.settings.color_bg != blank -%}
    background: {{ section.settings.color_bg }};
    {%- endif -%}
    {%- if section.settings.color_text != blank -%}
    color: {{ section.settings.color_text }};
    --crs-text:     {{ section.settings.color_text }};
    --crs-text-sec: {{ section.settings.color_text | color_modify: 'alpha', 0.6 }};
    --crs-text-ter: {{ section.settings.color_text | color_modify: 'alpha', 0.4 }};
    {%- endif -%}
    padding-top:    {{ section.settings.padding_top | default: 80 }}px;
    padding-bottom: {{ section.settings.padding_bottom | default: 80 }}px;
  }
{% endstyle %}

<section id="shopify-section-{{ section.id }}" class="sp">
  <div class="sp__container">

    {%- comment -%}=== Section Header ==={%- endcomment -%}
    <div class="sp__header crs-heading-stack crs-heading-stack--align-center{% if hp_headline_lg %} crs-heading-stack--headline-lg{% endif %} crs-heading-stack--homepage-truth">
      {%- if section.settings.kicker != blank -%}
        <p class="crs-heading-stack__kicker">{{ section.settings.kicker | escape }}</p>
      {%- endif -%}
      <div class="crs-heading-stack__pair">
        <h2 class="crs-heading-stack__title">{{ section.settings.headline | escape }}</h2>
      </div>
    </div>

    {%- comment -%}=== Testimonial Cards ==={%- endcomment -%}
    {%- assign has_testimonials = false -%}
    {%- for block in section.blocks -%}
      {%- if block.type == 'testimonial' -%}{%- assign has_testimonials = true -%}{%- break -%}{%- endif -%}
    {%- endfor -%}

    {%- if has_testimonials -%}
      <ul class="sp__grid" role="list">
        {%- for block in section.blocks -%}
          {%- if block.type == 'testimonial' -%}
            <li class="sp__card" {{ block.shopify_attributes }}>

              {%- comment -%} 1 — Author header (name top = person before quote) {%- endcomment -%}
              <div class="sp__card-header">
                {%- liquid
                  assign sp_avatar_raw = block.settings.avatar_image_url | strip
                  assign sp_avatar_src = blank
                  if sp_avatar_raw != blank
                    assign sp_avatar_src = block.settings.avatar_image_url | image_url: width: 80
                    if sp_avatar_src == blank
                      assign sp_avatar_src = sp_avatar_raw
                      if sp_avatar_src contains 'shopify://shop_images/'
                        assign sp_avatar_src = sp_avatar_src | remove: 'shopify://shop_images/' | file_url
                      elsif sp_avatar_src != blank
                        unless sp_avatar_src contains '://'
                          assign sp_avatar_src = sp_avatar_src | file_url
                        endunless
                      endif
                    endif
                  endif
                -%}
                <div class="sp__avatar" aria-hidden="true">
                  {%- if sp_avatar_src != blank -%}
                    <img src="{{ sp_avatar_src | escape }}" alt="{{ block.settings.name | escape }}" class="sp__avatar-img" loading="lazy" width="40" height="40">
                  {%- else -%}
                    <span class="sp__avatar-initial">{{ block.settings.name | slice: 0 | upcase }}</span>
                  {%- endif -%}
                </div>
                <div class="sp__author-meta">
                  <span class="sp__author-name">{{ block.settings.name | escape }}</span>
                  {%- if block.settings.verified -%}
                    <span class="sp__verified-badge">Begleitprogramm</span>
                  {%- endif -%}
                </div>
              </div>

              {%- comment -%} 2 — Quote {%- endcomment -%}
              {%- if block.settings.quote != blank -%}
                <blockquote class="sp__quote">
                  <p>{{ block.settings.quote | escape }}</p>
                </blockquote>
              {%- endif -%}

              {%- comment -%} 3 — Before / After with chronological age as context {%- endcomment -%}
              {%- if block.settings.result_metric != blank -%}
                <div class="sp__before-after" aria-label="Vorher-Nachher Ergebnis">
                  <div class="sp__ba-col sp__ba-col--before">
                    <span class="sp__ba-label">Vorher</span>
                    {%- if block.settings.age != blank -%}
                      <span class="sp__ba-chron">Chron. {{ block.settings.age }} J.</span>
                    {%- endif -%}
                    <span class="sp__ba-value">{{ block.settings.result_metric | escape }}</span>
                  </div>
                  {%- if block.settings.retest_result != blank -%}
                    <div class="sp__ba-col sp__ba-col--after">
                      <span class="sp__ba-label">Nachher</span>
                      {%- if block.settings.retest_duration != blank -%}
                        <span class="sp__ba-chron">{{ block.settings.retest_duration | escape }}</span>
                      {%- else -%}
                        <span class="sp__ba-chron">&nbsp;</span>
                      {%- endif -%}
                      <span class="sp__ba-value sp__ba-value--after">{{ block.settings.retest_result | escape }}</span>
                    </div>
                  {%- endif -%}
                </div>
              {%- endif -%}

            </li>
          {%- endif -%}
        {%- endfor -%}
      </ul>
    {%- endif -%}

    {%- comment -%}=== Testimonial Context Note ==={%- endcomment -%}
    {%- if section.settings.testimonial_context_note != blank -%}
      <p class="sp__context-note">{{ section.settings.testimonial_context_note | escape }}</p>
    {%- endif -%}

    {%- comment -%}=== Aggregate Stat Banner (up to 3 stats) ==={%- endcomment -%}
    {%- if section.settings.aggregate_stat_number != blank -%}
      {%- assign sp_has_stat2 = false -%}
      {%- assign sp_has_stat3 = false -%}
      {%- if section.settings.aggregate_stat_number_2 != blank -%}{%- assign sp_has_stat2 = true -%}{%- endif -%}
      {%- if section.settings.aggregate_stat_number_3 != blank -%}{%- assign sp_has_stat3 = true -%}{%- endif -%}
      <div class="sp__stat-banner{% if sp_has_stat2 %} sp__stat-banner--multi{% endif %}">
        <div class="sp__stat-item">
          <span class="sp__stat-number">{{ section.settings.aggregate_stat_number | escape }}</span>
          {%- if section.settings.aggregate_stat_label != blank -%}
            <p class="sp__stat-label">{{ section.settings.aggregate_stat_label | escape }}</p>
          {%- endif -%}
        </div>
        {%- if sp_has_stat2 -%}
          <div class="sp__stat-divider" aria-hidden="true"></div>
          <div class="sp__stat-item">
            <span class="sp__stat-number">{{ section.settings.aggregate_stat_number_2 | escape }}</span>
            {%- if section.settings.aggregate_stat_label_2 != blank -%}
              <p class="sp__stat-label">{{ section.settings.aggregate_stat_label_2 | escape }}</p>
            {%- endif -%}
          </div>
        {%- endif -%}
        {%- if sp_has_stat3 -%}
          <div class="sp__stat-divider" aria-hidden="true"></div>
          <div class="sp__stat-item">
            <span class="sp__stat-number">{{ section.settings.aggregate_stat_number_3 | escape }}</span>
            {%- if section.settings.aggregate_stat_label_3 != blank -%}
              <p class="sp__stat-label">{{ section.settings.aggregate_stat_label_3 | escape }}</p>
            {%- endif -%}
          </div>
        {%- endif -%}
        {%- if section.settings.aggregate_stat_sample_note != blank -%}
          <p class="sp__stat-sample-note">{{ section.settings.aggregate_stat_sample_note | escape }}</p>
        {%- endif -%}
      </div>
    {%- endif -%}

    {%- comment -%}=== Press Logos ==={%- endcomment -%}
    {%- if section.settings.show_press_logos -%}
      {%- assign has_logos = false -%}
      {%- for block in section.blocks -%}
        {%- if block.type == 'press_logo' -%}{%- assign has_logos = true -%}{%- break -%}{%- endif -%}
      {%- endfor -%}

      {%- if has_logos -%}
        <div class="sp__press">
          <p class="sp__press-label">Bekannt aus</p>
          <ul class="sp__press-row" role="list">
            {%- for block in section.blocks -%}
              {%- if block.type == 'press_logo' -%}
                {%- liquid
                  assign sp_logo_raw = block.settings.logo_image_url | strip
                  assign sp_logo_image_src = blank
                  if sp_logo_raw == blank
                    case block.settings.logo_alt
                      when 'RTL'
                        assign sp_logo_raw = '1_b5d8bee0-0588-4cab-8e38-b3eee8285532.png'
                      when 'Kölner Stadt Anzeiger'
                        assign sp_logo_raw = 'ksta-logo-grau-nobg.png'
                      when 'SWR1'
                        assign sp_logo_raw = 'swr1-logo.png'
                      when 'SAT.1'
                        assign sp_logo_raw = 'Kein_Titel_600_x_400_px_2.png'
                      when 'Spiegel Bestseller'
                        assign sp_logo_raw = '3_9df4d585-1a85-4c48-80bb-c8585a85cce7.png'
                    endcase
                  endif

                  if sp_logo_raw != blank
                    assign sp_logo_image_src = block.settings.logo_image_url | image_url: width: 240
                    if sp_logo_image_src == blank
                      assign sp_logo_image_src = sp_logo_raw
                    endif
                  endif

                  if sp_logo_image_src contains 'shopify://shop_images/'
                    assign sp_logo_image_src = sp_logo_image_src | remove: 'shopify://shop_images/' | file_url
                  elsif sp_logo_image_src != blank
                    unless sp_logo_image_src contains '://'
                      assign sp_logo_image_src = sp_logo_image_src | file_url
                    endunless
                  endif
                -%}
                <li class="sp__press-item" {{ block.shopify_attributes }}>
                  {%- if block.settings.logo_link_url != blank -%}
                    <a href="{{ block.settings.logo_link_url }}" class="sp__press-link" target="_blank" rel="noopener noreferrer" aria-label="{{ block.settings.logo_alt | escape }}">
                  {%- endif -%}
                  {%- if sp_logo_image_src != blank -%}
                    <img
                      src="{{ sp_logo_image_src | escape }}"
                      alt="{{ block.settings.logo_alt | escape }}"
                      class="sp__press-logo"
                      loading="lazy"
                      width="120"
                      height="40"
                    >
                  {%- else -%}
                    <span class="sp__press-logo-text">{{ block.settings.logo_alt | escape }}</span>
                  {%- endif -%}
                  {%- if block.settings.logo_link_url != blank -%}
                    </a>
                  {%- endif -%}
                </li>
              {%- endif -%}
            {%- endfor -%}
          </ul>
        </div>
      {%- endif -%}
    {%- endif -%}

  </div>
</section>

{% stylesheet %}
/* ================================================================
   lt-hp-social-proof — Homepage Social Proof + Presse
   Token refs: §3 (colors, spacing, type), §5 (Card Review)
   ================================================================ */

/* — Section shell — */
.sp {
  background: var(--color-surface-primary);
}

.sp__container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 var(--space-5);
}

/* — Section header — */
.sp__header {
  max-width: 640px;
  margin: 0 auto var(--space-8);   /* 64px → cards */
}


/* — Testimonial grid — */
/*
  DECISION: 3-col grid, no carousel (blueprint §5).
  Static cards stay visible — interaction with cards themselves
  introduces cognitive load between the user and the content.
*/
.sp__grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--space-5);             /* 24px */
  list-style: none;
  margin: 0 0 var(--space-8);     /* 64px → stat banner */
  padding: 0;
}

/* — Testimonial card (card-review pattern) — */
.sp__card {
  background: var(--color-surface-secondary);
  border: 1px solid var(--color-border-default);
  border-radius: var(--radius-md);
  padding: var(--space-5);        /* 24px */
  display: flex;
  flex-direction: column;
  gap: var(--space-4);
}


/* Quote text */
.sp__quote {
  flex: 1;
}

.sp__quote p {
  font-family: var(--font-body);
  font-size: var(--text-body);    /* 16px */
  font-weight: var(--weight-regular);
  line-height: var(--leading-relaxed);
  color: var(--color-text-primary);
  margin: 0;
}

/* — Card header: avatar + name + badge — */
.sp__card-header {
  display: flex;
  align-items: center;
  gap: var(--space-3);
  padding-bottom: var(--space-4);
  border-bottom: 1px solid var(--color-border-default);
}

/* Avatar circle — photo or initial fallback */
.sp__avatar {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  overflow: hidden;
  flex-shrink: 0;
  background: var(--color-surface-secondary);
  border: 1px solid var(--color-border-default);
  display: flex;
  align-items: center;
  justify-content: center;
}

.sp__avatar-img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

.sp__avatar-initial {
  font-family: var(--font-heading);
  font-size: var(--text-body-sm);
  font-weight: var(--weight-medium);
  color: var(--color-text-secondary);
  line-height: 1;
}

.sp__author-meta {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  font-family: var(--font-body);
  font-size: var(--text-body-sm);
  color: var(--color-text-secondary);
}

.sp__author-name {
  font-weight: var(--weight-medium);
  color: var(--color-text-primary);
}

.sp__verified-badge {
  display: inline-flex;
  align-items: center;
  margin-left: var(--space-2);
  margin-top: 0;
  padding: 2px var(--space-2);
  border-radius: var(--radius-pill);
  border: 1px solid var(--color-border-default);
  background: var(--color-surface-primary);
  color: var(--color-text-secondary);
  font-family: var(--font-body);
  font-size: var(--text-micro);
  font-weight: var(--weight-medium);
  line-height: var(--leading-tight);
  white-space: nowrap;
}

/* — Testimonial context note — */
.sp__context-note {
  font-family: var(--font-body);
  font-size: var(--text-body-sm);
  font-weight: var(--weight-regular);
  line-height: var(--leading-relaxed);
  color: var(--color-text-muted);
  text-align: center;
  max-width: 560px;
  margin: 0 auto var(--space-8);
  padding: 0 var(--space-4);
}

/* Before / After result block
   Two-column layout separated by a hairline border.
   Chron. age as context above bio age — shows the transformation clearly.
*/
.sp__before-after {
  display: flex;
  background: var(--color-surface-primary);
  border: 1px solid var(--color-border-default);
  border-radius: var(--radius-md);
  overflow: hidden;
}

.sp__ba-col {
  display: flex;
  flex-direction: column;
  gap: 2px;
  min-width: 0;
  flex: 1;
  padding: var(--space-3) var(--space-4);
}

.sp__ba-col--before {
  border-right: 1px solid var(--color-border-default);
}

.sp__ba-col--after {
  text-align: right;
}

.sp__ba-label {
  font-family: var(--font-body);
  font-size: var(--text-micro);
  font-weight: var(--weight-medium);
  line-height: var(--leading-tight);
  color: var(--color-text-muted);
  text-transform: uppercase;
  letter-spacing: 0.06em;
}

/* Contextual sub-label: chronological age (before) or duration (after) */
.sp__ba-chron {
  font-family: var(--font-body);
  font-size: 10px;
  font-weight: var(--weight-regular);
  line-height: var(--leading-tight);
  color: var(--color-text-muted);
  opacity: 0.75;
}

.sp__ba-value {
  font-family: var(--font-body);
  font-size: var(--text-body-sm);
  font-weight: var(--weight-semibold);
  line-height: var(--leading-tight);
  color: var(--color-text-secondary);
  overflow-wrap: break-word;
  word-break: break-word;
  margin-top: 2px;
}

.sp__ba-value--after {
  color: var(--color-text-primary);
}

/* — Aggregate Stat Banner —
   Single stat: centered column. Multi-stat (--multi modifier): row with dividers.
   Stat number in --text-stat (56px) commands the section.
*/
.sp__stat-banner {
  background: var(--color-surface-secondary);
  border: 1px solid var(--color-border-default);
  border-radius: var(--radius-lg);
  padding: var(--space-8) var(--space-6);
  margin-bottom: var(--space-8);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: var(--space-4);
}

.sp__stat-banner--multi {
  flex-direction: row;
  align-items: stretch;
  flex-wrap: wrap;
  gap: 0;
  padding: var(--space-7) var(--space-6);
}

.sp__stat-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: var(--space-3);
  flex: 1;
  min-width: 0;
  text-align: center;
  padding: var(--space-4) var(--space-5);
}

.sp__stat-divider {
  width: 1px;
  background: var(--color-border-default);
  align-self: stretch;
  flex-shrink: 0;
  margin: var(--space-4) 0;
}

.sp__stat-number {
  font-family: var(--font-heading);
  font-size: var(--text-stat);    /* 56px */
  font-weight: var(--weight-regular);
  line-height: var(--leading-stat);
  color: var(--color-text-primary);
  letter-spacing: -0.03em;
}

.sp__stat-banner--multi .sp__stat-number {
  font-size: var(--text-display); /* 48px */
  line-height: var(--leading-tight);
  letter-spacing: -0.02em;
}

.sp__stat-label {
  font-family: var(--font-body);
  font-size: var(--text-body);
  font-weight: var(--weight-regular);
  line-height: var(--leading-relaxed);
  color: var(--color-text-secondary);
  max-width: 240px;
  overflow-wrap: break-word;
  text-wrap: balance;
  margin: 0;
}

.sp__stat-sample-note {
  width: 100%;
  margin: var(--space-3) 0 0;
  text-align: center;
  font-family: var(--font-body);
  font-size: var(--text-micro);
  font-weight: var(--weight-regular);
  line-height: var(--leading-tight);
  color: var(--color-text-muted);
}

/* — Press logos —
   DECISION: Centered flex row, muted.
   Logo images are desaturated in CSS — keeps visual calm even with branded assets.
   Text fallback when image unavailable: styled as uppercase micro.
*/
.sp__press {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: var(--space-5);
}

.sp__press-label {
  font-family: var(--font-body);
  font-size: var(--text-micro);
  font-weight: var(--weight-medium);
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--color-text-muted);
}

.sp__press-row {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-wrap: wrap;
  gap: var(--space-6) var(--space-8);  /* 32px vert, 64px horiz */
  list-style: none;
  padding: 0;
  margin: 0;
}

.sp__press-item {
  display: flex;
  align-items: center;
}

.sp__press-link {
  display: block;
  text-decoration: none;
  transition: opacity var(--ease-default);
}

.sp__press-link:hover {
  opacity: 0.65;
}

.sp__press-logo {
  display: block;
  height: 32px;
  width: auto;
  max-width: 120px;
  object-fit: contain;
  filter: grayscale(1);
  opacity: 0.55;
  transition: opacity var(--ease-default), filter var(--ease-default);
}

.sp__press-link:hover .sp__press-logo,
.sp__press-logo:hover {
  opacity: 0.8;
  filter: grayscale(0);
}

/* Text fallback (no logo image) */
.sp__press-logo-text {
  font-family: var(--font-heading);
  font-size: var(--text-body-sm);
  font-weight: var(--weight-medium);
  letter-spacing: 0.05em;
  color: var(--color-text-muted);
  white-space: nowrap;
}

/* — Responsive — */
@media screen and (max-width: 1024px) {
  .sp__grid {
    grid-template-columns: 1fr 1fr;
  }

  .sp__stat-banner--multi .sp__stat-number {
    font-size: var(--text-h1);
  }
}

@media screen and (max-width: 768px) {
  .sp__container {
    padding: 0 var(--space-4);
  }

  .sp__header {
    margin-bottom: var(--space-7);
  }

  .sp__grid {
    grid-template-columns: 1fr;
    gap: var(--space-4);
    margin-bottom: var(--space-7);
  }

  .sp__stat-banner {
    padding: var(--space-7) var(--space-4);
    margin-bottom: var(--space-7);
  }

  .sp__stat-banner--multi {
    flex-direction: column;
    gap: 0;
    padding: var(--space-5) var(--space-4);
  }

  .sp__stat-divider {
    width: calc(100% - (var(--space-4) * 2));
    max-width: 100%;
    height: 1px;
    margin: 0 auto;
    align-self: center;
  }

  .sp__stat-item {
    padding: var(--space-5) var(--space-4);
  }

  .sp__stat-number {
    font-size: var(--text-display);  /* 48px on mobile */
  }

  .sp__stat-label {
    max-width: 320px;
  }

  .sp__press-row {
    gap: var(--space-5) var(--space-6);
  }

  .sp__author-meta {
    align-items: flex-start;
    flex-direction: column;
  }

  .sp__verified-badge {
    margin-left: 0;
    margin-top: var(--space-1);
  }
}
{% endstylesheet %}

{% schema %}
{
  "name": "HP Social Proof",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "header",
      "content": "Abstände"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Abstand oben",
      "min": 0,
      "max": 200,
      "step": 8,
      "default": 80,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Abstand unten",
      "min": 0,
      "max": 200,
      "step": 8,
      "default": 80,
      "unit": "px"
    },
    {
      "type": "header",
      "content": "Farben"
    },
    {
      "type": "color",
      "id": "color_bg",
      "label": "Hintergrundfarbe"
    },
    {
      "type": "color",
      "id": "color_text",
      "label": "Textfarbe"
    },
    {
      "type": "header",
      "content": "Inhalt"
    },
    {
      "type": "select",
      "id": "heading_size",
      "label": "Ueberschrift Groesse",
      "options": [
        { "value": "section", "label": "Standard (Abschnitt)" },
        { "value": "product_title", "label": "Wie Produkttitel" }
      ],
      "default": "section"
    },
    {
      "type": "text",
      "id": "kicker",
      "label": "Kicker"
    },
    {
      "type": "text",
      "id": "headline",
      "label": "Headline"
    },
    {
      "type": "header",
      "content": "Testimonial Kontext"
    },
    {
      "type": "textarea",
      "id": "testimonial_context_note",
      "label": "Kontextnotiz unter den Karten",
      "info": "Erklärt den Begleitprogramm-Kontext. Erscheint klein und muted zwischen Karten und Statistik-Banner."
    },
    {
      "type": "header",
      "content": "Statistik 1"
    },
    {
      "type": "text",
      "id": "aggregate_stat_number",
      "label": "Zahl",
      "info": "z.B. 87 %"
    },
    {
      "type": "textarea",
      "id": "aggregate_stat_label",
      "label": "Beschreibung"
    },
    {
      "type": "header",
      "content": "Statistik 2 (optional)"
    },
    {
      "type": "text",
      "id": "aggregate_stat_number_2",
      "label": "Zahl"
    },
    {
      "type": "textarea",
      "id": "aggregate_stat_label_2",
      "label": "Beschreibung"
    },
    {
      "type": "header",
      "content": "Statistik 3 (optional)"
    },
    {
      "type": "text",
      "id": "aggregate_stat_number_3",
      "label": "Zahl"
    },
    {
      "type": "textarea",
      "id": "aggregate_stat_label_3",
      "label": "Beschreibung"
    },
    {
      "type": "text",
      "id": "aggregate_stat_sample_note",
      "label": "Hinweis unter den Statistiken (optional)",
      "info": "z. B. 'Basis: Befragung von 24 Kund*innen'"
    },
    {
      "type": "header",
      "content": "Presse"
    },
    {
      "type": "checkbox",
      "id": "show_press_logos",
      "label": "Presse-Logos anzeigen",
      "default": true
    }
  ],
  "blocks": [
    {
      "type": "testimonial",
      "name": "Testimonial",
      "settings": [
        {
          "type": "image_picker",
          "id": "avatar_image_url",
          "label": "Foto (optional)",
          "info": "Quadratisches Bild, wird als Kreis angezeigt. Leer = Initiale."
        },
        {
          "type": "textarea",
          "id": "quote",
          "label": "Zitat"
        },
        {
          "type": "text",
          "id": "name",
          "label": "Name"
        },
        {
          "type": "text",
          "id": "age",
          "label": "Chronologisches Alter",
          "info": "Nur Zahl, z.B. 44 — wird als Kontext im Vorher/Nachher-Feld angezeigt"
        },
        {
          "type": "text",
          "id": "result_metric",
          "label": "Ergebnis Ersttest (Vorher)",
          "info": "Kurzform, z. B. «Bio-Alter: 64 Jahre»"
        },
        {
          "type": "text",
          "id": "retest_result",
          "label": "Ergebnis Retest (Nachher)",
          "info": "Kurzform, z. B. «Bio-Alter: 60 Jahre». Leer = kein Vorher→Nachher."
        },
        {
          "type": "text",
          "id": "retest_duration",
          "label": "Zeitraum Retest",
          "info": "z. B. «nach 16 Monaten». Erscheint als Label über dem Nachher-Wert."
        },
        {
          "type": "text",
          "id": "protocol_label",
          "label": "Longevity Protokoll (optional)",
          "info": "Einträge mit · trennen, z. B. «Longevity Protokoll · NAD⁺ Level erhöhen · Ernährung · Schlaf · Retest nach 12 Monaten». Jeder Teil wird als eigener Tag angezeigt."
        },
        {
          "type": "checkbox",
          "id": "verified",
          "label": "Begleitprogramm-Badge anzeigen",
          "info": "Zeigt 'Begleitprogramm' — für Kund*innen aus dem intensiven Begleitprogramm",
          "default": true
        }
      ]
    },
    {
      "type": "press_logo",
      "name": "Presse-Logo",
      "settings": [
        {
          "type": "image_picker",
          "id": "logo_image_url",
          "label": "Logo Bild",
          "info": "Leer lassen -> Standard-Presse-Logo aus den PDPs wird anhand des Namens verwendet"
        },
        {
          "type": "text",
          "id": "logo_alt",
          "label": "Name / Alt-Text"
        },
        {
          "type": "url",
          "id": "logo_link_url",
          "label": "Link URL (optional)"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "HP Social Proof",
      "blocks": [
        {
          "type": "testimonial",
          "settings": {
            "quote": "Mein biologisches Alter lag 7 Jahre unter meinem chronologischen. Ich dachte, ich mache bereits alles richtig – jetzt weiß ich, warum.",
            "name": "Markus T.",
            "age": "44",
            "result_metric": "Biologisches Alter: 37 · Chronologisch: 44",
            "verified": true
          }
        },
        {
          "type": "testimonial",
          "settings": {
            "quote": "Die Speichelprobe war in drei Minuten erledigt. Wie verständlich die App alles erklärt – ich habe keine Biologie studiert, aber ich verstehe jetzt meine Werte.",
            "name": "Sophie K.",
            "age": "38",
            "result_metric": "",
            "verified": true
          }
        },
        {
          "type": "testimonial",
          "settings": {
            "quote": "Als Arzt war ich skeptisch. Aber Eurofins, ISO 17025 und die Methodik hinter dem MethylPace-Score haben mich überzeugt. Das ist kein Consumer-Gadget.",
            "name": "Dr. med. Andreas F.",
            "age": "",
            "result_metric": "",
            "verified": true
          }
        },
        { "type": "press_logo", "settings": { "logo_alt": "SWR1", "logo_image_url": "shopify://shop_images/swr1-logo.png" } },
        { "type": "press_logo", "settings": { "logo_alt": "Spiegel Bestseller", "logo_image_url": "shopify://shop_images/3_9df4d585-1a85-4c48-80bb-c8585a85cce7.png" } },
        { "type": "press_logo", "settings": { "logo_alt": "RTL", "logo_image_url": "shopify://shop_images/1_b5d8bee0-0588-4cab-8e38-b3eee8285532.png" } },
        { "type": "press_logo", "settings": { "logo_alt": "SAT.1", "logo_image_url": "shopify://shop_images/Kein_Titel_600_x_400_px_2.png" } }
      ]
    }
  ]
}
{% endschema %}
