{% comment %}
  lt-lp-value-stack
  Surface: white value clarification
  Dominant element: the included-value list and the single investment row
  CTA: none
  Quality move: the section reads like a completeness manifest, not a discount comparison
{% endcomment %}

{% style %}
  #lt-lp-value-stack-{{ section.id }} {
    --lt-lp-stack-bg: {{ section.settings.color_bg }};
    --lt-lp-stack-surface: {{ section.settings.color_surface }};
    --lt-lp-stack-text: {{ section.settings.color_text | default: '#26251e' }};
    --lt-lp-stack-muted: {{ section.settings.color_text | default: '#26251e' | color_modify: 'alpha', 0.64 }};
    --lt-lp-stack-border: {{ section.settings.color_text | default: '#26251e' | color_modify: 'alpha', 0.12 }};
    --lt-lp-stack-padding-top: {{ section.settings.padding_top }}px;
    --lt-lp-stack-padding-bottom: {{ section.settings.padding_bottom }}px;
  }
{% endstyle %}

<section id="lt-lp-value-stack-{{ section.id }}" class="lt-lp-stack">
  <div class="lt-lp-stack__inner page-width">
    <div class="lt-lp-stack__header">
      {%- if section.settings.stack_heading != blank -%}
        <h2 class="lt-lp-stack__heading">{{ section.settings.stack_heading | escape }}</h2>
      {%- endif -%}
      {%- if section.settings.stack_subheading != blank -%}
        <p class="lt-lp-stack__subheading">{{ section.settings.stack_subheading | escape }}</p>
      {%- endif -%}
    </div>

    <div class="lt-lp-stack__list">
      {%- for block in section.blocks -%}
        {%- if block.type == 'stack_item' -%}
          <div class="lt-lp-stack__item" {{ block.shopify_attributes }}>
            <div class="lt-lp-stack__item-copy">
              <div class="lt-lp-stack__item-header">
                <p class="lt-lp-stack__item-title">{{ block.settings.item_title | escape }}</p>
                {%- if block.settings.item_label != blank -%}
                  <span class="lt-lp-stack__item-pill">{{ block.settings.item_label | escape }}</span>
                {%- endif -%}
              </div>
              {%- if block.settings.item_description != blank -%}
                <p class="lt-lp-stack__item-description">{{ block.settings.item_description | escape }}</p>
              {%- endif -%}
            </div>
          </div>
        {%- endif -%}
      {%- endfor -%}
    </div>

    <aside class="lt-lp-stack__summary">
      <div class="lt-lp-stack__summary-row">
        <p class="lt-lp-stack__summary-label">{{ section.settings.stack_final_label | escape }}</p>
        <p class="lt-lp-stack__summary-value">{{ section.settings.stack_final_value | escape }}</p>
      </div>
      {%- if section.settings.stack_final_meta != blank -%}
        <p class="lt-lp-stack__summary-meta">{{ section.settings.stack_final_meta | escape }}</p>
      {%- endif -%}
    </aside>

    {%- if section.settings.stack_footnote != blank -%}
      <p class="lt-lp-stack__footnote">{{ section.settings.stack_footnote | escape }}</p>
    {%- endif -%}
  </div>
</section>

{% stylesheet %}
.lt-lp-stack {
  background: var(--lt-lp-stack-bg);
  color: var(--lt-lp-stack-text);
}

.lt-lp-stack__inner {
  padding-top: var(--lt-lp-stack-padding-top);
  padding-bottom: var(--lt-lp-stack-padding-bottom);
}

.lt-lp-stack__header {
  max-width: 42rem;
  margin-bottom: var(--space-6);
}

.lt-lp-stack__heading {
  margin: 0;
  font-family: var(--font-heading);
  font-size: clamp(var(--text-h2), 4vw, var(--text-display));
  font-weight: var(--weight-bold);
  line-height: var(--leading-heading);
  color: var(--lt-lp-stack-text);
}

.lt-lp-stack__subheading,
.lt-lp-stack__footnote {
  margin: var(--space-3) 0 0;
  font-family: var(--font-body);
  line-height: var(--leading-relaxed);
}

.lt-lp-stack__subheading {
  font-size: var(--text-body-lg);
  color: var(--lt-lp-stack-muted);
}

.lt-lp-stack__list,
.lt-lp-stack__summary {
  background: var(--lt-lp-stack-surface);
  border: 1px solid var(--lt-lp-stack-border);
  border-radius: var(--radius-lg);
}

.lt-lp-stack__list {
  padding: var(--space-5);
}

.lt-lp-stack__item {
  padding-block: var(--space-4);
  border-bottom: 1px solid var(--lt-lp-stack-border);
}

.lt-lp-stack__item:first-child {
  padding-top: 0;
}

.lt-lp-stack__item:last-child {
  padding-bottom: 0;
  border-bottom: 0;
}

.lt-lp-stack__item-header {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-3);
  align-items: center;
}

.lt-lp-stack__item-title,
.lt-lp-stack__item-description,
.lt-lp-stack__summary-label,
.lt-lp-stack__summary-value,
.lt-lp-stack__summary-meta {
  margin: 0;
}

.lt-lp-stack__item-title {
  font-family: var(--font-body);
  font-size: var(--text-body);
  font-weight: var(--weight-semibold);
  line-height: var(--leading-body);
  color: var(--lt-lp-stack-text);
}

.lt-lp-stack__item-pill {
  display: inline-flex;
  align-items: center;
  min-height: 1.75rem;
  padding-inline: var(--space-3);
  border: 1px solid var(--lt-lp-stack-border);
  border-radius: var(--radius-full);
  font-family: var(--font-body);
  font-size: var(--text-micro);
  font-weight: var(--weight-semibold);
  line-height: var(--leading-tight);
  color: var(--lt-lp-stack-text);
  white-space: nowrap;
}

.lt-lp-stack__item-description {
  margin-top: var(--space-2);
  font-family: var(--font-body);
  font-size: var(--text-body-sm);
  line-height: var(--leading-relaxed);
  color: var(--lt-lp-stack-muted);
}

.lt-lp-stack__summary {
  margin-top: var(--space-5);
  padding: var(--space-5);
}

.lt-lp-stack__summary-row {
  display: flex;
  justify-content: space-between;
  gap: var(--space-4);
  align-items: end;
}

.lt-lp-stack__summary-label {
  font-family: var(--font-body);
  font-size: var(--text-body-sm);
  font-weight: var(--weight-semibold);
  line-height: var(--leading-body);
  color: var(--lt-lp-stack-text);
}

.lt-lp-stack__summary-value {
  font-family: var(--font-heading);
  font-size: clamp(var(--text-h2), 4vw, var(--text-display));
  font-weight: var(--weight-bold);
  line-height: var(--leading-heading);
  color: var(--lt-lp-stack-text);
  text-align: end;
}

.lt-lp-stack__summary-meta {
  margin-top: var(--space-2);
  font-family: var(--font-body);
  font-size: var(--text-body-sm);
  line-height: var(--leading-body);
  color: var(--lt-lp-stack-muted);
}

.lt-lp-stack__footnote {
  font-size: var(--text-body-sm);
  color: var(--lt-lp-stack-muted);
}

@media screen and (max-width: 749px) {
  .lt-lp-stack__subheading {
    font-size: var(--text-body);
  }

  .lt-lp-stack__list,
  .lt-lp-stack__summary {
    padding: var(--space-4);
  }

  .lt-lp-stack__summary-row {
    flex-direction: column;
    align-items: start;
  }

  .lt-lp-stack__summary-value {
    text-align: start;
  }

  .lt-lp-stack__item-header {
    gap: var(--space-2);
    align-items: start;
  }
}
{% endstylesheet %}

{% schema %}
{
  "name": "LP Value Stack",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "color",
      "id": "color_bg",
      "label": "Background",
      "default": "#f7f7f4"
    },
    {
      "type": "color",
      "id": "color_surface",
      "label": "Surface",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "color_text",
      "label": "Text",
      "default": "#26251e"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding top",
      "min": 0,
      "max": 160,
      "step": 8,
      "unit": "px",
      "default": 48
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding bottom",
      "min": 0,
      "max": 160,
      "step": 8,
      "unit": "px",
      "default": 64
    },
    {
      "type": "text",
      "id": "stack_heading",
      "label": "Heading",
      "default": "Was im Preis enthalten ist."
    },
    {
      "type": "text",
      "id": "stack_subheading",
      "label": "Subheading",
      "default": "Kein Abo. Keine Upsells. Kein Kleingedrucktes."
    },
    {
      "type": "text",
      "id": "stack_final_label",
      "label": "Final label",
      "default": "Einmalige Investition"
    },
    {
      "type": "text",
      "id": "stack_final_value",
      "label": "Final value",
      "default": "349 €"
    },
    {
      "type": "text",
      "id": "stack_final_meta",
      "label": "Final meta",
      "default": "kein Abo · versandkostenfrei · DSGVO"
    },
    {
      "type": "textarea",
      "id": "stack_footnote",
      "label": "Footnote",
      "default": "Lebenslanger Zugang zur LIFETIME App · Neue Analyse-Bereiche werden automatisch verfügbar, sobald wir sie hinzufügen · Kein Upgrade-Zwang."
    }
  ],
  "blocks": [
    {
      "type": "stack_item",
      "name": "Stack Item",
      "limit": 6,
      "settings": [
        {
          "type": "text",
          "id": "item_title",
          "label": "Title",
          "default": "DNA-Analyse mit 187 Reports"
        },
        {
          "type": "text",
          "id": "item_label",
          "label": "Label",
          "default": "Enthalten"
        },
        {
          "type": "text",
          "id": "item_description",
          "label": "Description",
          "default": "16 Kategorien von Fitness bis Sensitivitäten"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "LP Value Stack",
      "blocks": [
        {
          "type": "stack_item",
          "settings": {
            "item_title": "DNA-Analyse mit 187 Reports",
            "item_label": "Enthalten",
            "item_description": "16 Kategorien von Fitness bis Sensitivitäten"
          }
        },
        {
          "type": "stack_item",
          "settings": {
            "item_title": "Epigenetische Analyse (MethylPace-Score)",
            "item_label": "Enthalten",
            "item_description": "6 Bereiche, darunter biologisches Alter und Zell-Vitalität"
          }
        },
        {
          "type": "stack_item",
          "settings": {
            "item_title": "Longevity-Erstberatung (30 Min)",
            "item_label": "Enthalten",
            "item_description": "Mit einem unserer Longevity-Experten, optional per Video"
          }
        },
        {
          "type": "stack_item",
          "settings": {
            "item_title": "LIFETIME App mit AI Health Coach",
            "item_label": "Lebenslang",
            "item_description": "Ergebnisse, Einordnung, konkrete Empfehlungen"
          }
        },
        {
          "type": "stack_item",
          "settings": {
            "item_title": "Face Scan Hautanalyse",
            "item_label": "Enthalten",
            "item_description": "Visuelle Altersindikatoren und Hautgesundheit"
          }
        },
        {
          "type": "stack_item",
          "settings": {
            "item_title": "Eurofins Laboranalyse",
            "item_label": "ISO-zertifiziert",
            "item_description": "Dieselben Standards wie klinische Diagnostik"
          }
        }
      ]
    }
  ]
}
{% endschema %}
