{% comment %}
  lt-lp-value-stack
  Surface: white value clarification
  Dominant element: the final value comparison
  CTA: none
  Quality move: itemized value makes the price feel grounded without turning into a sales box
{% endcomment %}

{% style %}
  #lt-lp-value-stack-{{ section.id }} {
    --lt-lp-stack-bg: {{ section.settings.color_bg }};
    --lt-lp-stack-surface: {{ section.settings.color_surface }};
    --lt-lp-stack-text: {{ section.settings.color_text | default: '#26251e' }};
    --lt-lp-stack-muted: {{ section.settings.color_text | default: '#26251e' | color_modify: 'alpha', 0.64 }};
    --lt-lp-stack-border: {{ section.settings.color_text | default: '#26251e' | color_modify: 'alpha', 0.12 }};
    --lt-lp-stack-padding-top: {{ section.settings.padding_top }}px;
    --lt-lp-stack-padding-bottom: {{ section.settings.padding_bottom }}px;
  }
{% endstyle %}

<section id="lt-lp-value-stack-{{ section.id }}" class="lt-lp-stack">
  <div class="lt-lp-stack__inner page-width">
    <div class="lt-lp-stack__header">
      {%- if section.settings.stack_heading != blank -%}
        <h2 class="lt-lp-stack__heading">{{ section.settings.stack_heading | escape }}</h2>
      {%- endif -%}
      {%- if section.settings.stack_subheading != blank -%}
        <p class="lt-lp-stack__subheading">{{ section.settings.stack_subheading | escape }}</p>
      {%- endif -%}
    </div>

    <div class="lt-lp-stack__layout">
      <div class="lt-lp-stack__list">
        {%- for block in section.blocks -%}
          {%- if block.type == 'stack_item' -%}
            <div class="lt-lp-stack__item" {{ block.shopify_attributes }}>
              <div class="lt-lp-stack__item-copy">
                <p class="lt-lp-stack__item-title">{{ block.settings.item_title | escape }}</p>
                {%- if block.settings.item_label != blank -%}
                  <p class="lt-lp-stack__item-label">{{ block.settings.item_label | escape }}</p>
                {%- endif -%}
              </div>
              {%- if block.settings.item_value != blank -%}
                <p class="lt-lp-stack__item-value">{{ block.settings.item_value | escape }}</p>
              {%- endif -%}
            </div>
          {%- endif -%}
        {%- endfor -%}
      </div>

      <aside class="lt-lp-stack__summary">
        <div class="lt-lp-stack__summary-row">
          <p class="lt-lp-stack__summary-label">{{ section.settings.stack_total_label | escape }}</p>
          <p class="lt-lp-stack__summary-value">{{ section.settings.stack_total_value | escape }}</p>
        </div>
        <div class="lt-lp-stack__summary-row lt-lp-stack__summary-row--final">
          <p class="lt-lp-stack__summary-label">{{ section.settings.stack_final_label | escape }}</p>
          <p class="lt-lp-stack__summary-value">{{ section.settings.stack_final_value | escape }}</p>
        </div>
      </aside>
    </div>

    {%- if section.settings.stack_footnote != blank -%}
      <p class="lt-lp-stack__footnote">{{ section.settings.stack_footnote | escape }}</p>
    {%- endif -%}
  </div>
</section>

{% stylesheet %}
.lt-lp-stack {
  background: var(--lt-lp-stack-bg);
  color: var(--lt-lp-stack-text);
}

.lt-lp-stack__inner {
  padding-top: var(--lt-lp-stack-padding-top);
  padding-bottom: var(--lt-lp-stack-padding-bottom);
}

.lt-lp-stack__header {
  max-width: 42rem;
  margin-bottom: var(--space-6);
}

.lt-lp-stack__heading {
  margin: 0;
  font-family: var(--font-heading);
  font-size: clamp(var(--text-h2), 4vw, var(--text-display));
  font-weight: var(--weight-bold);
  line-height: var(--leading-heading);
  color: var(--lt-lp-stack-text);
}

.lt-lp-stack__subheading,
.lt-lp-stack__footnote {
  margin: var(--space-3) 0 0;
  font-family: var(--font-body);
  line-height: var(--leading-relaxed);
}

.lt-lp-stack__subheading {
  font-size: var(--text-body-lg);
  color: var(--lt-lp-stack-muted);
}

.lt-lp-stack__layout {
  display: grid;
  gap: var(--space-5);
}

.lt-lp-stack__list,
.lt-lp-stack__summary {
  background: var(--lt-lp-stack-surface);
  border: 1px solid var(--lt-lp-stack-border);
  border-radius: var(--radius-lg);
}

.lt-lp-stack__list {
  padding: var(--space-5);
}

.lt-lp-stack__item {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  gap: var(--space-3);
  align-items: end;
  padding-block: var(--space-4);
  border-bottom: 1px solid var(--lt-lp-stack-border);
}

.lt-lp-stack__item:first-child {
  padding-top: 0;
}

.lt-lp-stack__item:last-child {
  padding-bottom: 0;
  border-bottom: 0;
}

.lt-lp-stack__item-title,
.lt-lp-stack__item-value,
.lt-lp-stack__summary-label,
.lt-lp-stack__summary-value {
  margin: 0;
}

.lt-lp-stack__item-title {
  font-family: var(--font-body);
  font-size: var(--text-body);
  font-weight: var(--weight-semibold);
  line-height: var(--leading-body);
  color: var(--lt-lp-stack-text);
}

.lt-lp-stack__item-label {
  margin: var(--space-1) 0 0;
  font-family: var(--font-body);
  font-size: var(--text-body-sm);
  line-height: var(--leading-body);
  color: var(--lt-lp-stack-muted);
}

.lt-lp-stack__item-value {
  font-family: var(--font-heading);
  font-size: var(--text-section);
  font-weight: var(--weight-bold);
  line-height: var(--leading-tight);
  color: var(--lt-lp-stack-text);
  white-space: nowrap;
}

.lt-lp-stack__summary {
  padding: var(--space-5);
}

.lt-lp-stack__summary-row {
  display: flex;
  justify-content: space-between;
  gap: var(--space-4);
  align-items: end;
}

.lt-lp-stack__summary-row + .lt-lp-stack__summary-row {
  margin-top: var(--space-5);
  padding-top: var(--space-5);
  border-top: 1px solid var(--lt-lp-stack-border);
}

.lt-lp-stack__summary-label {
  font-family: var(--font-body);
  font-size: var(--text-body-sm);
  font-weight: var(--weight-semibold);
  line-height: var(--leading-body);
  color: var(--lt-lp-stack-muted);
}

.lt-lp-stack__summary-value {
  font-family: var(--font-heading);
  font-size: clamp(var(--text-h2), 4vw, var(--text-display));
  font-weight: var(--weight-bold);
  line-height: var(--leading-heading);
  color: var(--lt-lp-stack-text);
  text-align: end;
}

.lt-lp-stack__summary-row--final .lt-lp-stack__summary-label,
.lt-lp-stack__summary-row--final .lt-lp-stack__summary-value {
  color: var(--lt-lp-stack-text);
}

.lt-lp-stack__footnote {
  font-size: var(--text-body-sm);
  color: var(--lt-lp-stack-muted);
}

@media screen and (min-width: 990px) {
  .lt-lp-stack__layout {
    grid-template-columns: minmax(0, 1.35fr) minmax(18rem, 0.8fr);
  }
}

@media screen and (max-width: 749px) {
  .lt-lp-stack__subheading {
    font-size: var(--text-body);
  }

  .lt-lp-stack__item {
    grid-template-columns: 1fr;
  }

  .lt-lp-stack__item-value,
  .lt-lp-stack__summary-value {
    white-space: normal;
    text-align: start;
  }

  .lt-lp-stack__summary-row {
    flex-direction: column;
    align-items: start;
  }
}
{% endstylesheet %}

{% schema %}
{
  "name": "LP Value Stack",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "color",
      "id": "color_bg",
      "label": "Background",
      "default": "#f7f7f4"
    },
    {
      "type": "color",
      "id": "color_surface",
      "label": "Surface",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "color_text",
      "label": "Text",
      "default": "#26251e"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding top",
      "min": 0,
      "max": 160,
      "step": 8,
      "unit": "px",
      "default": 48
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding bottom",
      "min": 0,
      "max": 160,
      "step": 8,
      "unit": "px",
      "default": 64
    },
    {
      "type": "text",
      "id": "stack_heading",
      "label": "Heading",
      "default": "Was du einzeln dafür zahlen würdest."
    },
    {
      "type": "text",
      "id": "stack_subheading",
      "label": "Subheading",
      "default": "Bei vergleichbaren Anbietern. Transparent aufgeschlüsselt."
    },
    {
      "type": "text",
      "id": "stack_total_label",
      "label": "Total label",
      "default": "Zusammen einzeln gekauft"
    },
    {
      "type": "text",
      "id": "stack_total_value",
      "label": "Total value",
      "default": "~537 €"
    },
    {
      "type": "text",
      "id": "stack_final_label",
      "label": "Final label",
      "default": "Bei LIFETIME einmalig"
    },
    {
      "type": "text",
      "id": "stack_final_value",
      "label": "Final value",
      "default": "349 €"
    },
    {
      "type": "textarea",
      "id": "stack_footnote",
      "label": "Footnote",
      "default": "Vergleichswerte basieren auf durchschnittlichen Marktpreisen vergleichbarer Anbieter (Stand 2026). Der AGE & DNA-Test ersetzt keine medizinische Diagnostik."
    }
  ],
  "blocks": [
    {
      "type": "stack_item",
      "name": "Stack Item",
      "limit": 6,
      "settings": [
        {
          "type": "text",
          "id": "item_title",
          "label": "Title",
          "default": "DNA-Analyse (187 Marker, 16 Kategorien)"
        },
        {
          "type": "text",
          "id": "item_value",
          "label": "Value",
          "default": "~199 €"
        },
        {
          "type": "text",
          "id": "item_label",
          "label": "Label"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "LP Value Stack",
      "blocks": [
        {
          "type": "stack_item",
          "settings": {
            "item_title": "DNA-Analyse (187 Marker, 16 Kategorien)",
            "item_value": "~199 €"
          }
        },
        {
          "type": "stack_item",
          "settings": {
            "item_title": "Epigenetische Analyse (6 Bereiche, MethylPace-Score)",
            "item_value": "~249 €"
          }
        },
        {
          "type": "stack_item",
          "settings": {
            "item_title": "Longevity-Beratung (Erstgespräch, 30 Min)",
            "item_value": "~89 €"
          }
        },
        {
          "type": "stack_item",
          "settings": {
            "item_title": "LIFETIME App + AI Health Coach (lebenslang)",
            "item_value": "—",
            "item_label": "Inklusive"
          }
        },
        {
          "type": "stack_item",
          "settings": {
            "item_title": "Face Scan Hautanalyse",
            "item_value": "—",
            "item_label": "Inklusive"
          }
        }
      ]
    }
  ]
}
{% endschema %}
