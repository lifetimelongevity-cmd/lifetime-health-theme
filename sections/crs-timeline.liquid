{% style %}
  #section-{{ section.id }} {
    --tl-text:     {{ settings.color_text_main | default: '#26251e' }};
    --tl-text-sec: {{ settings.color_text_main | color_modify: 'alpha', 0.65 | default: 'rgba(38,37,30,0.65)' }};
    --tl-text-ter: {{ settings.color_text_main | color_modify: 'alpha', 0.4 | default: 'rgba(38,37,30,0.4)' }};
    --tl-bg:       {{ settings.color_background_main | default: '#f7f7f4' }};
    --tl-card-bg:  {{ settings.color_background_main | color_modify: 'lightness', -3 | default: '#f2f1ed' }};
    --tl-border:   {{ settings.color_text_main | color_modify: 'alpha', 0.12 | default: 'rgba(38,37,30,0.12)' }};
    --tl-dot-active: {{ settings.color_text_main | color_modify: 'alpha', 0.8 | default: 'rgba(38,37,30,0.8)' }};
    padding-top:    {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
  }
{% endstyle %}

<div class="tl-section" id="section-{{ section.id }}">
  <div class="tl-container">
    <div class="tl-layout">

      {%- comment -%} Left: Timeline {%- endcomment -%}
      <div class="tl-left">

        {%- if section.settings.kicker != blank -%}
          <p class="tl-kicker">{{ section.settings.kicker | escape }}</p>
        {%- endif -%}

        {%- if section.settings.headline != blank -%}
          <h2 class="tl-headline">{{ section.settings.headline | escape }}</h2>
        {%- endif -%}

        {%- if section.blocks.size > 0 -%}
          <div class="tl-items">
            {%- for block in section.blocks -%}
              <div class="tl-item{% if block.settings.is_active %} tl-item--active{% endif %}" {{ block.shopify_attributes }}>

                {%- if block.settings.time_label != blank -%}
                  <span class="tl-badge">{{ block.settings.time_label | escape }}</span>
                {%- endif -%}

                {%- if block.settings.step_title != blank -%}
                  <h3 class="tl-step-title">{{ block.settings.step_title | escape }}</h3>
                {%- endif -%}

                {%- if block.settings.step_body != blank -%}
                  <p class="tl-step-body">{{ block.settings.step_body | escape }}</p>
                {%- endif -%}

                {%- if block.settings.show_callout_box and block.settings.callout_text != blank -%}
                  <div class="tl-callout">{{ block.settings.callout_text | escape }}</div>
                {%- endif -%}

              </div>
            {%- endfor -%}
          </div>
        {%- endif -%}

      </div>

      {%- comment -%} Right: Image or placeholder {%- endcomment -%}
      <div class="tl-right">
        {%- if section.settings.image != blank -%}
          {{
            section.settings.image
            | image_url: width: 1000
            | image_tag:
              loading: 'lazy',
              class: 'tl-image',
              alt: section.settings.image_alt | escape
          }}
        {%- else -%}
          <div class="tl-placeholder"></div>
        {%- endif -%}
      </div>

    </div>
  </div>
</div>

{% stylesheet %}
/* ===================================================
   Timeline Split — crs-timeline.liquid
   =================================================== */

.tl-section {
  background: var(--tl-bg, #f7f7f4);
}

.tl-container {
  max-width: 1280px;
  margin: 0 auto;
  padding: 0 20px;
}

/* Layout */
.tl-layout {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 48px;
  align-items: start;
}

/* Left */
.tl-kicker {
  font-size: 12px;
  font-weight: 400;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--tl-text-ter, rgba(38,37,30,0.4));
  margin: 0 0 12px;
}

.tl-headline {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Helvetica, Arial, sans-serif !important;
  font-size: 22px !important;
  font-weight: 400 !important;
  line-height: 1.3 !important;
  letter-spacing: -0.3px !important;
  color: var(--tl-text, #26251e) !important;
  margin: 0 0 28px !important;
}

/* Timeline items */
.tl-items {
  position: relative;
  padding-left: 24px;
  border-left: 1px solid var(--tl-border, rgba(38,37,30,0.12));
}

.tl-item {
  position: relative;
  margin-bottom: 32px;
}

.tl-item:last-child {
  margin-bottom: 0;
}

/* Dot via pseudo-element */
.tl-item::before {
  content: '';
  position: absolute;
  left: -30px;
  top: 4px;
  width: 10px;
  height: 10px;
  border-radius: 50%;
  background: var(--tl-card-bg, #f2f1ed);
  border: 1.5px solid rgba(38,37,30,0.3);
  box-sizing: border-box;
}

.tl-item--active::before {
  background: var(--tl-dot-active, rgba(38,37,30,0.8));
  border-color: var(--tl-dot-active, rgba(38,37,30,0.8));
}

/* Time badge */
.tl-badge {
  display: inline-block;
  font-size: 11px;
  font-weight: 400;
  color: var(--tl-text-sec, rgba(38,37,30,0.65));
  background: var(--tl-card-bg, #f2f1ed);
  border: 1px solid var(--tl-border, rgba(38,37,30,0.1));
  border-radius: 20px;
  padding: 2px 10px;
  margin-bottom: 6px;
}

/* Step title */
.tl-step-title {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Helvetica, Arial, sans-serif !important;
  font-size: 16px !important;
  font-weight: 500 !important;
  line-height: 1.4 !important;
  letter-spacing: 0 !important;
  color: var(--tl-text, #26251e) !important;
  margin: 0 0 4px !important;
}

/* Step body */
.tl-step-body {
  font-size: 14px;
  font-weight: 400;
  line-height: 1.65;
  color: var(--tl-text-sec, rgba(38,37,30,0.65));
  margin: 0;
}

/* Callout */
.tl-callout {
  background: rgba(38,37,30,0.05);
  border-left: 2px solid rgba(38,37,30,0.2);
  border-radius: 4px;
  padding: 10px 12px;
  margin-top: 8px;
  font-size: 13px;
  line-height: 1.6;
  color: var(--tl-text-sec, rgba(38,37,30,0.65));
}

/* Right image */
.tl-right {
  position: sticky;
  top: 80px;
}

.tl-image {
  width: 100%;
  height: auto;
  border-radius: 4px;
  object-fit: cover;
  display: block;
}

.tl-placeholder {
  width: 100%;
  aspect-ratio: 4 / 3;
  background: var(--tl-card-bg, #f2f1ed);
  border-radius: 4px;
}

/* Mobile */
@media (max-width: 1024px) {
  .tl-layout {
    grid-template-columns: 1fr;
    gap: 32px;
  }

  .tl-right {
    position: static;
    order: -1;
  }
}
{% endstylesheet %}

{% schema %}
{
  "name": "Timeline Split",
  "tag": "section",
  "class": "crs-timeline",
  "settings": [
    {
      "type": "text",
      "id": "kicker",
      "label": "Kicker (optional)",
      "default": "WAS KANNST DU ERWARTEN?"
    },
    {
      "type": "text",
      "id": "headline",
      "label": "Überschrift",
      "default": "Aus Forschung wird Alltag"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Bild (rechte Spalte)"
    },
    {
      "type": "text",
      "id": "image_alt",
      "label": "Bild Alt-Text"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Abstand oben",
      "min": 0,
      "max": 120,
      "step": 8,
      "unit": "px",
      "default": 80
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Abstand unten",
      "min": 0,
      "max": 120,
      "step": 8,
      "unit": "px",
      "default": 80
    }
  ],
  "blocks": [
    {
      "type": "step",
      "name": "Zeitpunkt",
      "settings": [
        {
          "type": "text",
          "id": "time_label",
          "label": "Zeit-Badge",
          "default": "Nach 4 Wochen"
        },
        {
          "type": "text",
          "id": "step_title",
          "label": "Titel",
          "default": "Mehr Energie"
        },
        {
          "type": "textarea",
          "id": "step_body",
          "label": "Beschreibung",
          "default": "NAD⁺-Stoffwechsel: erste messbare Veränderungen. Viele berichten von einem konstanteren Energiegefühl im Alltag."
        },
        {
          "type": "checkbox",
          "id": "is_active",
          "label": "Aktiver Zeitpunkt (gefüllter Punkt)",
          "default": true
        },
        {
          "type": "checkbox",
          "id": "show_callout_box",
          "label": "Callout-Box anzeigen",
          "default": false
        },
        {
          "type": "textarea",
          "id": "callout_text",
          "label": "Callout-Text",
          "default": "Nach Absetzen: NAD⁺ fällt in 4 Wochen auf Ausgangswert zurück — Kontinuität ist entscheidend."
        }
      ]
    }
  ],
  "max_blocks": 6,
  "presets": [
    {
      "name": "Timeline Split",
      "blocks": [
        {
          "type": "step",
          "settings": {
            "time_label": "Nach 4 Wochen",
            "step_title": "Mehr Energie",
            "step_body": "NAD⁺-Stoffwechsel: erste messbare Veränderungen. Viele berichten von einem konstanteren Energiegefühl im Alltag.",
            "is_active": true,
            "show_callout_box": false
          }
        },
        {
          "type": "step",
          "settings": {
            "time_label": "Nach 2 Monaten",
            "step_title": "Spürbar fitter",
            "step_body": "Ausdauer und Erholung verbessern sich. Erste Rückmeldungen über tieferen Schlaf und weniger Erschöpfung.",
            "is_active": true,
            "show_callout_box": false
          }
        },
        {
          "type": "step",
          "settings": {
            "time_label": "Nach 3 Monaten",
            "step_title": "Herz & Stoffwechsel",
            "step_body": "Studien zeigen Verbesserungen bei Blutdruckwerten und metabolischen Markern nach 90 Tagen konsistenter Einnahme.",
            "is_active": true,
            "show_callout_box": false
          }
        },
        {
          "type": "step",
          "settings": {
            "time_label": "Nach 4 Monaten",
            "step_title": "Deep Longevity Benefits",
            "step_body": "DNA-Reparaturmechanismen, mitochondriale Funktion und zellulare Regeneration auf messbarem Niveau aktiv.",
            "is_active": true,
            "show_callout_box": true,
            "callout_text": "Nach Absetzen: NAD⁺ fällt in 4 Wochen auf Ausgangswert zurück — Kontinuität ist entscheidend."
          }
        }
      ]
    }
  ]
}
{% endschema %}
