---
description: LIFETIME design tokens — always active for all files
globs: ["**/*.liquid", "**/*.css", "**/*.html", "**/*.js"]
alwaysApply: true
---

## Design Tokens

Canonical definition: `docs/design-governance.md §3`

Always use that file as the single source of truth for all CSS custom properties and design tokens.
